/** 本文件承载 AI 回答的引用展示；没有真实 Citation 数据时不渲染任何内容。 */

/**
 * 当前历史接口尚未提供 Citation 数据，因此保持空输出。
 *
 * 这样没有引用的回答不会出现误导性的“暂无可展示的引用”；
 * 等真实引用契约接入后，再由这里负责渲染来源列表。
 */
export function AiCitationList() {
  return null;
}
