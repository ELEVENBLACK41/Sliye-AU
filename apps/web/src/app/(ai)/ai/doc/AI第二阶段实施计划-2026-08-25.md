# NextNest AI 第二阶段实施计划：跨业务 Agent 状态基建

> 文档状态：实施基线 v2.28，2.1～2.5 已完成；2.6-A～D 代码已完成，2.6-E 真实写操作仍待人工门禁；2.7-A～D 已完成，2.7-E～F 代码完成待真实浏览器验收
> 创建日期：2026-08-25  
> 当前阶段：2.7-F 新会话首发与路由保持；2.7-E～F 代码与自动化门禁已完成，真实已有/新会话浏览器验收待执行
> 上位文档：`Decision Agent与RAG可测试实施路线图-2026-08-17.md`  
> 文档用途：单独维护第二阶段的产品边界、状态模型、数据约束、实施顺序、完成状态和验收门禁。后续第二阶段以本文为直接实施基线；本文未评审通过前不进入 Prisma migration。

---

## 1. 状态标记

| 标记 | 含义 |
| --- | --- |
| ✅ 已完成 | 代码和约定已经落地，相关门禁已通过 |
| 🟡 部分完成 | 已有可复用实现，但仍有缺口、需要修正或尚未通过全部门禁 |
| 🧪 待验证 | 实现已经存在，但当前环境未能完成验证，不能按已完成计算 |
| ⬜ 未开始 | 尚未进入本步骤，或者当前只有占位文件 |
| ⛔ 阻塞 | 在问题解决前不得进入依赖它的后续步骤 |

完成状态必须以代码、测试和门禁为依据，不能因为已有 UI、占位目录或类型名称就标记为完成。

---

## 2. 本阶段结论

第二阶段不只为“单项决策聊天”建立状态表，而要为最终的全局 Decision Agent 建立可扩展的运行基建。

最终 Agent 需要能够：

- 查询当前用户有权访问的项目、决策、提案、投票、会议、通话和参与关系；
- 回答“哪些决策讨论过 Redis”“最长会议开了多久”“上次和谁通话”“有哪些投票需要我参与”等跨模块问题；
- 比较多个项目或提案，并明确比较维度和证据来源；
- 根据讨论、提案、投票、会议和正式决议生成可交互的决策线路图；
- 后续在明确审批后执行受控业务操作，但第二阶段仍只实现只读闭环；
- 对所有工具调用、来源、状态变化、错误、取消和模型用量进行审计。

因此，`AiThread` 不绑定也不保存项目、决策、会议等业务上下文。Thread 只是归属于创建用户的对话容器；用户在消息中直接说明要查询、比较或绘制的对象，Agent 再通过发现/搜索工具定位真实业务实体。目标重名或信息不足时，Agent 返回候选项并追问，不把猜测写成隐含会话状态。

### 2.1 第二阶段负责什么

- 修正并冻结 Thread、Message、Run、Event 的共享语义；
- 建立 PostgreSQL 权威状态和事务约束；
- 建立幂等、单 Thread 单活跃 Run、事件序号、执行租约和迟到写入 fencing；
- 支持同一 Thread 的用户输入持久化排队，以及“调整方向”触发的当前 Run 有序取消与优先续跑；
- 用一条最小真实、只读、受 NestJS 权限保护的业务工具链（实体发现 + 决策上下文读取）验证运行闭环；
- 提供 Thread 历史、消息分页、停止、重试、归档和恢复接口；
- 按照现有 AI 工作台的第一版 UI 和视觉风格，接入真实 Thread、Run、工具、引用与页面状态；
- 支持刷新恢复、同进程断线事件补拉和确定性故障收敛。

### 2.2 第二阶段明确不负责什么

- 不在本阶段完成全部项目、投票、会议、提案和检索工具；
- 不实现“讨论过 Redis”所需的完整关键词、向量和混合检索；
- 不引入 `pgvector`、Redis、BullMQ、Rerank 或独立向量数据库；
- 不实现写业务数据的 Agent 操作；
- 不实现跨进程续跑、Serverless 长任务恢复和生产级端到端取消；
- 不实现多个 Run 并行执行、跨 Thread 全局调度或把新指令直接注入已经开始的模型请求；
- 不实现会话全文搜索、文件夹、共享会话、物理删除或复杂保留策略；
- 不重新设计或替换现有 AI 工作台，不为了接入旧业务逻辑而回退到旧版 Card、列表、表单或布局；
- 不因为已有网页检索或 Mock 工具而把第二阶段视为真实业务闭环完成。

这些能力分别在后续只读工具、结构化证据、关键词检索、向量检索、生产加固和审批阶段继续实现。

---

## 3. 当前实现审计

审计时间：2026-08-25；已于 2026-08-26 独立复核 2.5 的实现、范围和可复现验证。

### 3.1 前置阶段

| 项目 | 状态 | 当前证据 | 仍需处理 |
| --- | --- | --- | --- |
| 第 0 阶段评测合同与数据集 | 🟡 部分完成 | 已有 50 条 Gold Query、脱敏 Fixture、指标、Mock Runner 和版本化报告 | 当前环境需要重新跑通 Server 评测和类型检查，Mock 报告不能代表真实业务检索质量 |
| 第 1 阶段模型注册表与 Gateway | ✅ 已完成 | 已有模型注册表、角色预算、Gateway 回退、错误归一化、用量/成本遥测；Web 模型测试 8/8 通过 | 真实 Gateway Smoke Test 仍按需显式执行，不作为每轮单测 |

### 3.2 第二阶段总览

| 步骤 | 当前状态 | 已有内容 | 主要缺口 |
| --- | --- | --- | --- |
| 2.1 契约与状态规则 | ✅ 已完成 | Thread 已改为用户所有且不持久化任何业务目标；创建请求只包含首条消息与幂等键；稳定错误码和 Run 状态矩阵测试已通过 | 无；Prisma、HTTP、真实工具和 UI 数据接入按计划留给后续步骤 |
| 2.2 持久化与事务地基 | ✅ 已完成 | 已落地 Prisma 核心模型、原子创建、幂等、事件追加、持久化输入队列、定向替代、队首领取和真实 PostgreSQL 并发测试 | 执行器租约、定向取消请求与 fencing 留给 2.3 |
| 2.3 租约、取消与故障收敛 | ✅ 已完成 | 已实现领取、续租、fencing、停止/取消、调整方向有序分发与过期租约对账；13 条真实 PostgreSQL 竞争与故障测试通过 | 无 |
| 2.4 真实单 Thread 运行闭环 | ✅ 已完成 | 最小工具链、声明式串联规则、步骤/工具/来源审计、内部执行接口、Web Runtime、SSE/BFF、进程级对账与后继 Run 串行续跑均已落地；2.4-M 修复了评审发现的孤儿排队 Run、事件补拉参数校验、截断摘要重放、重放租约 fencing 和维护循环 5 个问题，并已通过真实 PostgreSQL 回归 | 真实模型供应商的人工 Smoke 留作部署验收，不影响本步骤代码和数据库门禁 |
| 2.5 会话历史与权限接口 | ✅ 已完成 | Thread 列表/详情、消息分页、置顶、重命名/归档/恢复、来源失权实时判定与投影、BFF 路由与旁路越权回归均已落地 | 无；真实引用生成因 `AiCitation` 表未建立留给后续阶段 |
| 2.6 真实数据接入与阶段验收 | 🧪 待验证 | 2.6-A～D 代码已接入真实 Thread、消息、Run、队列和会话元数据；2.6-E 已完成新会话入口、视觉/滚动检查、只读深链接/Thread 切换/归档列表浏览器验收，以及 Node 22 自动化门禁 | 仍需在隔离测试会话或明确授权的真实账号上执行发送/排队/调整方向/停止/重试/置顶/重命名保存/归档恢复的浏览器写操作，并完成断线重连与来源失权 Fixture 验收 |
| 2.7 POST 直出流与持久化恢复双通道 | 🟡 2.7-A 已按 UI-first 修订，B～D 已完成，E～F 代码完成待浏览器验收，I 灰度/正式回滚后置 | 已冻结 `live-delta` 即时帧、`liveDeltaId/liveSequence` 关联、异步 `AiEvent` 确认/恢复语义；NestJS 已返回事务事件回执，Web Runtime 已接入按 Run 串行、有界、终态前冲刷的异步持久化队列与故障隔离 live sink；已有 Thread 和新 Thread 的 POST SSE 已接入现有 reducer、稳定布局协调器和 GET 恢复流 | 需先完成 2.7-E 已有 Thread 与 2.7-F 新会话浏览器验收；2.7-G～H 尚未执行；2.7-I 的 feature flag、灰度和正式回滚不作为当前个人项目门槛 |

### 3.3 当前验证结果

2026-08-26 的 2.5 独立复验结果：

- 首次完整运行暴露两项测试基础设施缺口：独立库 `Sliye_test` 不存在，以及本地 Prisma Client 未随 `20260825153337_add_ai_thread_pinned_at` 重新生成；前者导致集成测试无法连库，后者使运行时 Client 缺少 `AiThread.pinnedAt` 字段。两项均已修复：用 `test:db:create` 确认/创建独立库、向测试库 `migrate deploy` 24 条现有 migration，并执行 `prisma generate`；未触碰开发库数据。
- `pnpm --filter @nextnest/server test -- --runInBand modules/ai`：15 个 Suite、166 条测试全部通过；其中 `ai-persistence.integration.spec.ts` 在独立 PostgreSQL 测试库中运行。
- `pnpm --filter @workspace/contracts check-types`：通过；`pnpm --filter @nextnest/web exec tsc --noEmit`：通过。
- 本机执行时 pnpm 报告当前 Node 为 **18.20.3**，而根 `package.json` 要求 `>=22.21.1 <23`。上述结果足以证明当前测试逻辑可运行，但不等同于目标 Node 22 的运行时验收；合并/发布前仍须在 Node 22 环境复跑。
- 复核结论：2.5 的后端与 BFF 实现达到计划内的自动化门禁；但当前 `test:db:create` 只负责建库，不负责迁移，且 Prisma Client 生成物会漂移。进入 2.6 前应把“建库 → migrate deploy → prisma generate → AI 定向测试”写成可重复的单一测试准备命令或 CI 步骤，避免再次出现环境就绪假象。

2026-08-26 的 2.6-B 定向验证结果：

- 新增事件 reducer 与消息适配回归：6 条 Web Node 测试全部通过，覆盖 `runId + sequence` 去重、乱序缓存、工具失败收敛、未知事件保守忽略、实时正文适配、失败状态保留和来源失权中性占位；
- 本轮修改文件定向 ESLint 通过；
- `pnpm --filter @nextnest/web exec tsc --noEmit` 已执行，本轮新增文件没有类型错误，仍被 `.next/types/validator.ts` 中 5 个已删除 AI 路由的残留声明阻塞；
- 尚未执行真实账户浏览器验证，因此 2.6-B 当前标记为 🧪 待验证；断线重连、两个 Thread 切换隔离和桌面/移动端滚动属于后续人工/浏览器验收项；
- 本轮未修改 NestJS Runtime、Prisma、事件表或 SSE 协议，也未接入发送、停止、重试和真实 Citation。

2026-08-27 的 2.7-A 定向验证结果：

- 新增 `@workspace/contracts/ai` POST 直出流协议联合类型，冻结 `submission`、`ai-event`、`run-status`、`stream-handoff` 和 `stream-error` 五类帧，以及提交元数据、恢复游标和流错误结构；
- 新增纯 SSE 编解码器，覆盖任意网络 chunk 边界、LF/CRLF、JSON 换行转义、未知事件、缺少 data 和不完整帧；
- 本轮没有修改 Route Handler、NestJS、Agent Runtime、Prisma、数据库 migration 或现有 GET SSE；
- 改造前基线已记录当前 Runtime `120` 字符/`400ms` 批处理、GET SSE `400ms` 轮询、首段可见时间分解、事件间隔/文本长度和空轮询的可复现统计口径；未把未采集的真实模型延迟伪装成实测值。

2026-08-27 的 2.7-B 定向验证结果：

- 文本增量内部接口在事务提交后返回完整 `AiEvent`，不再只返回 `sequence`；工具开始/结束和 Run 终态回执携带本次事务新提交事件及最后序号；
- 公共停止接口仍返回原有最小 `runId/status/nextRunId` 结构，内部工具输入和审计事件不会通过浏览器控制接口泄漏；
- Server AI 全量定向回归通过（15 Suite / 167 Tests），其中真实 PostgreSQL 持久化集成 31 条、工具编排 11 条；contracts 类型检查、Web 类型检查和修改文件 ESLint/Prettier 均通过；
- 未修改 Prisma schema、历史 migration、Route Handler、现有 GET SSE 或浏览器推流，未创建一次性测试文件，也没有需要删除的无用测试资产。

2026-08-27 的 2.7-C 定向验证结果：

- 新增进程内 `AiRuntimeLiveSink`，支持事件/状态顺序写出、主动关闭、有界缓冲、慢消费者关闭和写入失败隔离；`startAiAgentRun` / `runAiAgentExecution` 支持可选 sink；
- Runtime 已提交文本、工具和终态事件均按回执发布，终态状态快照在事件之后发布；无 sink 时继续走原后台执行路径；文本批次初始调整为 `40` 字符或 `100ms`；
- Web Runtime 测试通过（13/13），覆盖 live sink 顺序、主动断开、慢消费者和写入失败；Web TypeScript、定向 ESLint 和 Prettier 检查通过；
- 未接入浏览器 POST 流，不修改 Route Handler、GET SSE、Prisma schema、migration 或数据库行为；新增 live sink 测试属于长期防回归资产，没有需要删除的一次性测试文件。

2026-08-27 的 2.7 UI-first 协议调整记录：

- 根据老大确认，将原“提交后推送”改为“模型 delta 先通过 `live-delta` 直出，`AiEvent` 异步持久化并作为恢复确认来源”；
- 为即时增量与持久化文本事件冻结 `liveDeltaId + liveSequence` 关联规则，明确前端不得因确认事件重复追加正文，恢复只按持久化 `runId + sequence` 补拉；
- 扩展现有 live sink 消息形状以承载即时 delta，但尚未切换 Runtime 发布逻辑，也未实现异步持久化队列或 2.7-D 浏览器 POST；
- 当前 `40/100` 只保留为数据库持久化批次候选，不再作为前端渲染控制；本轮未新增 Markdown，未删除长期回归测试。

2026-08-27 的 2.7-C UI-first 实现结果：

- Runtime 对每个模型非空 delta 立即生成 `liveDeltaId/liveSequence` 并通过 live sink 发布，前端展示不再等待数据库；
- 新增按 Run 串行、有界的异步文本持久化队列，按候选批次写入带 `liveDeltaIds/liveSequenceStart/liveSequenceEnd` 的 `AiEvent`，数据库提交成功后发布持久化确认；
- Run 正常终态前等待队列冲刷；队列溢出或持久化失败触发中止并阻止正常完成，live sink 关闭/慢消费者/写入失败不影响 Runtime 持久化与终态处理；
- Web Runtime 测试通过（16/16），协议测试 3/3，contracts/Web 类型检查、定向 ESLint/Prettier 和 diff 检查通过；Server AI 全量定向回归 15 Suite / 168 Tests 通过，真实 PostgreSQL 持久化集成 32 条；
- 未接入浏览器 POST Route、未修改 Prisma schema、migration、GET SSE 或前端 UI；未新增 Markdown，新增队列测试属于长期防回归资产，没有需要删除的一次性测试。

2026-08-27 的 2.7-D 定向验证结果：

- `[threadId]/messages` 在 `Accept: text/event-stream` 下复用既有 NestJS 幂等提交，返回 `submission` 后把当前 Run 的 live delta、持久化事件、终态状态和恢复 handoff 通过同一 POST SSE 响应输出；未带该 Accept 时保留原 JSON 响应与 Runtime `after()` 派发路径；排队提交只返回 `submission`，不伪造活跃流；
- 新增 POST 响应桥测试 4 条，覆盖正常顺序、排队、客户端断开后 Runtime 继续完成和流开始后的 Runtime 异常；与协议 3 条、live sink 3 条合计 10 条通过；
- Web TypeScript 类型检查、修改文件 ESLint 和 `git diff --check` 通过；
- 直出 Runtime 只消费当前 Run，后继排队 Run 重新进入无订阅者后台链；未修改 NestJS、Prisma、GET SSE、新会话路由或 UI；
- 未运行 Prettier CLI：当前 workspace 未安装可解析的 Prettier 可执行文件，代码风格由现有 ESLint 和手工 diff 检查确认。

2026-08-27 的 2.7-E 定向验证结果：

- 新增浏览器 POST SSE Transport：复用 `requestResponse` 的 BFF 认证刷新链路，按 `submission`、`live-delta`、`ai-event`、`run-status`、`stream-handoff` 和 `stream-error` 分发，提交 Promise 与完整流 Promise 分离；
- 现有 reducer 新增 live delta 入口，并通过 `liveDeltaId` 记录即时展示/持久化确认，避免同一正文重复追加；已有 Thread 提交时本地投影用户消息和活跃 Run，POST 主流期间屏蔽重复 GET SSE，异常或 handoff 后才恢复 GET SSE；
- 新增 Transport 回归 3 条、Reducer 回归 2 条；与既有 Runtime 20 条、协议/工作区相关测试合计本轮定向通过；Web TypeScript、修改文件 ESLint 和 `git diff --check` 通过；
- 真实已有 Thread 浏览器发送、长 Markdown/代码块渲染、断流 handoff 和最终历史一致性尚未验收，2.7-E 暂标记为 🧪；未修改新会话首发、聊天 UI、Prisma 或灰度部署配置。

2026-08-27 的 2.7-F 定向验证结果：

- 新会话 `POST /api/ai/threads` 在 `Accept: text/event-stream` 下复用统一 POST SSE 协议，收到 `submission.threadId` 后再执行 URL 切换；不带该请求头时保留原 JSON + `after()` 路径；
- 新增 `(ai)` 稳定布局 Provider 托管 POST 读取器和事件回放，`/ai` 与 `/ai/:threadId` Hook 通过同一协调器接管流，不在路由切换时复制或中止当前读取器；
- 新增新会话 Transport 路径回归，已有 Runtime、Transport、Reducer、协议和工作区相关测试合计 37 条通过；Web TypeScript、修改文件 ESLint 和 `git diff --check` 通过；
- 真实新会话首发、URL 切换、快速切换/刷新恢复和最终历史一致性尚未验收，2.7-F 暂标记为 🧪；未修改聊天 UI、Prisma 或灰度部署配置。

2026-08-27 的 2.7 部署策略调整：

- 根据老大确认，当前个人项目不引入 `AI_DIRECT_STREAM_ENABLED`、灰度发布和正式自动回滚流程；已有 Thread 通过 `Accept: text/event-stream` 选择 POST 直出，不带该请求头时继续使用原 JSON + GET SSE 路径；
- JSON 兼容路径和 GET SSE 保留为低成本兼容/手动回退能力，不将其包装为独立灰度系统；未来出现真实线上用户、多实例部署或发布风险时，再单独评估 feature flag、观测和回滚门禁。

2026-08-25 的定向验证结果：

- `pnpm --filter @nextnest/web test:ai:model`：8/8 通过（第一阶段既有验证）；
- `pnpm --filter @workspace/contracts check-types`：通过；
- `pnpm --filter @nextnest/server test -- --runInBand modules/ai/state/ai-run.machine.spec.ts`：1 个 Test Suite、56 条测试全部通过；已撤销入口上下文设计的 11 条测试随对应实现一并删除；
- `pnpm --filter @nextnest/server test -- --runInBand modules/ai/state/ai-run.machine.spec.ts modules/ai/services/ai-persistence.integration.spec.ts`：2 个 Test Suite、62 条测试全部通过；其中 6 条真实 PostgreSQL 用例覆盖输入排队、FIFO 领取、幂等重放与定向替代；
- `20260825100000_add_ai_input_queue` 已应用到 `Sliye`，并通过 `prisma migrate status`；测试清理后 AI Thread、Message、Run、Event 均为 0 条；
- 定向命令在沙箱内第一次因无法读取已有 TypeScript 依赖而失败，获得允许后在沙箱外重跑通过；该问题不是业务代码失败，也不阻塞 2.1；
- 本轮按改动风险只执行上述定向类型检查和状态规则测试，没有运行全量 build 或无关评测集。

本轮（2.4-M 评审修复）验证情况：

- AI 模块单测 12 个 Suite、119 条通过（含新增的截断摘要约定与重放拒绝用例）；
- 真实 PostgreSQL 持久化集成测试 16 条通过：新增 1 条“从未被领取的孤儿排队 Run 会在宽限期后被对账重新交回派发”，并在既有工具审计用例内补充“同一调用重放返回既有结果”与“租约失效后拒绝重放”两条断言；既有 15 条全部保持通过，说明本轮修复没有引入回归；
- contracts、Web `tsc --noEmit` 与 AI 模块 ESLint 全部通过；Server `tsc --noEmit` 仍只被既有 `meetings.service.spec.ts` 两处 fixture 字段缺失阻塞；
- 默认测试命令补上 `--experimental-vm-modules` 后，Server 全量 Jest 由“2 个 Suite 失败”改善为“1 个失败”，剩余失败为既有 `meetings.service.spec.ts` 的 3 条会议时间断言，与 AI 模块无关；
- 本轮回归临时借用开发库执行：执行前已确认 7 张 AI 表均为空行，集成测试 `afterEach` 的无条件 `deleteMany()` 无数据可删，非 AI 的 department/project/decision fixture 在 `finally` 中按 id 精确清理。后续接入真实会话数据前必须改用独立测试库；
- 没有运行全量 build。

上一轮（完整 2.4）验证情况：

- `20260825140000_add_ai_step_tool_call_source` 已应用，Prisma Client 已重新生成；
- 2.4 定向单测 10 个 Suite、51 条通过；AI 状态机 68 条通过；Web Runtime/模型测试 10 条通过；
- 真实 PostgreSQL 持久化集成测试 15 条通过，覆盖 Run 竞争/故障、步骤与工具关联、遗留运行中工具收敛，以及执行中撤销项目成员身份后后续工具调用被拒绝；
- contracts 类型检查、Web `tsc --noEmit` 与本轮修改文件 ESLint 通过；Server `tsc --noEmit` 仍只被既有 `meetings.service.spec.ts` 两处 fixture 字段缺失阻塞，本轮 AI 文件没有新增类型错误；
- 没有运行全量 build；本轮按 AI Runtime、权限、事务与跨端契约风险执行了直接相关的验证。

上一轮（完整 2.3）验证结果：

- `pnpm --filter @nextnest/server test -- --runInBand modules/ai/services/ai-persistence.integration.spec.ts`：13 条真实 PostgreSQL 用例全部通过；测试结束后仅清理 AI 会话相关表与临时测试用户；
- `pnpm --filter @nextnest/server test -- --runInBand modules/ai/state/ai-run.machine.spec.ts`：56 条状态机测试通过；
- 本轮 AI 服务与测试文件的 ESLint 通过；`pnpm --filter @workspace/contracts check-types` 通过；
- 全量 Server `tsc --noEmit` 已执行，失败于既有 `meetings.service.spec.ts` 的两处 fixture 缺少 `invitationStatus`、`respondedAt`，本轮 AI 文件没有类型错误；
- 未运行全量 build 或迁移；本步骤复用已存在的租约、失败和取消字段，未修改 Prisma schema，因此无需生成 migration。

---

## 4. 已确认的核心设计

以下内容来自当前产品目标，先作为第二阶段不可破坏的设计约束。

1. Agent 的默认产品形态是跨业务数据助手，不是单 Decision Chat。
2. Thread 不保存项目、决策、会议等业务上下文，也不提供手动选择业务目标的入口。
3. 用户直接通过自然语言说明目标；Agent 使用发现/搜索工具解析实体，遇到歧义时展示候选项并追问。
4. 所有工具调用都由 NestJS 根据当前用户和实时业务关系重新鉴权。
5. 精确事实通过结构化领域工具查询，不能让模型或向量检索猜测。
6. 讨论、消息和会议转写等非结构化证据在后续检索阶段接入。
7. PostgreSQL 保存 Thread、Run、事件和审计事实；Redis 将来只承担任务调度、缓存和实时协调。
8. 同一 Thread 第一版同时最多一个占用执行门禁的非终态 Run；用户消息可以持久化排队，但排队项不等同于并发 Run。
9. “调整方向”不是把文本硬插入已开始的模型请求，而是持久化一条高优先级用户消息、请求取消当前 Run，并在取消收敛后优先创建下一次 Run。
10. Run 进入终态后不可回退；重试必须创建新 Run 并关联旧 Run。
11. 浏览器断开不等于取消。事件先持久化，再推送给订阅者。
12. 模型生成物默认为草稿，不得自动写入正式业务记录。
13. 第二阶段只开放“实体发现 + 决策上下文读取”这一条最小真实只读工具链验证闭环，不提前铺开提案、投票、讨论、会议或检索工具集。
14. 当前 AI 工作台第一版 UI 是第二阶段的数据接入和视觉验收基线；后续只按现有风格填充真实能力，不另起一套页面设计。

---

## 5. Agent 目标解析与来源权限方案

### 5.1 Thread 只保存对话状态

`AiThread` 只保存所有者、标题、当前活跃 Run、归档时间和审计时间，不保存项目、决策、会议或通话 ID。创建请求也只包含首条用户消息与幂等键：

```ts
type CreateAiThreadRequest = {
  message: string;
  idempotencyKey: string;
};
```

页面将来若提供“询问 AI”快捷入口，也只负责生成一条明确的自然语言消息，例如“总结决策《缓存方案评审》的 Redis 讨论”，而不是给 Thread 设置隐藏业务状态。这样后续追问和跨项目比较都由可见对话承载，不产生过期的默认目标。

### 5.2 Agent 如何定位业务目标

1. Agent 从当前用户消息和必要的最近对话中提取实体名称、类型、时间范围和查询意图；
2. 精确 ID 已在消息可见内容中给出时，可直接调用对应的窄工具；
3. 只有名称或描述时，先调用受权限保护的发现/搜索工具获得候选实体；
4. 唯一候选才继续查询；多个合理候选或置信度不足时，返回不泄漏越权数据的候选项并追问；
5. 工具结果返回稳定来源标识，回答只引用本次 Run 实际获准读取的来源。

第二阶段只实现一条最小真实工具链，不在 2.1 提前实现完整的目标解析器或搜索工具。2.4 允许用户直接以自然语言给出决策名称、别名或可见标识符，Agent 先调用 `findDecisionCandidates`，唯一命中后再调用 `getDecisionContext`；重名或信息不足时只展示允许访问的候选并追问。明确 `decisionId` 可以作为测试 Fixture，但不是产品输入要求。第 3 阶段在这条链路上扩展跨业务实体发现与更多结构化工具；讨论正文、会议转写等非结构化证据的“讨论过 Redis”类问题，再由第 6、7 阶段检索能力逐步补齐。

### 5.3 权限与历史保护

- 当前用户身份只进入 Agent Runtime 的受控执行上下文，不作为模型可以伪造的工具参数；
- 每次 NestJS 工具调用都根据当前用户和实时业务关系鉴权，不信任模型或浏览器传入的派生范围；
- 每个 Run/助手消息登记实际读取和实际引用的来源依赖；
- 读取历史时重新校验这些来源；某个来源失权时，只锁定或隐藏受影响的助手消息、工具结果和引用，并显示中性占位；
- Thread 本身没有业务根上下文，因此不会因单个项目、决策或会议失权而整体锁死，用户仍可继续询问其他有权数据；
- 权限撤销发生在 Run 执行中时，NestJS 使当前租约失效并以 `SOURCE_ACCESS_REVOKED` 收敛 Run，撤销后的事件和结果不得继续落库或推送；
- 系统管理员也继续遵守现有私有区域访问规则，不自动穿透私有讨论；
- 是否允许来源恢复后自动解锁对应历史，需要在 2.5 前单独评审。

这套策略需要 `AiSourceDependency` 或等价关系表支持，不能只在 Message JSON 中保存不可查询的来源列表。

---

## 6. Run 状态与并发不变量

当前状态集合继续保留：

```text
QUEUED
  -> RUNNING
  -> CANCELLED

RUNNING
  -> WAITING_APPROVAL
  -> CANCELLATION_REQUESTED
  -> COMPLETED
  -> FAILED

WAITING_APPROVAL
  -> RUNNING
  -> CANCELLATION_REQUESTED

CANCELLATION_REQUESTED
  -> CANCELLED
  -> FAILED

CANCELLED / COMPLETED / FAILED
  -> 不允许继续转换
```

第二阶段必须同时保证：

- 同状态重复写入由事务幂等层处理，不放宽状态机；
- 同一 Thread 最多一个 `QUEUED/RUNNING/WAITING_APPROVAL/CANCELLATION_REQUESTED` Run；
- `completed -> running` 永远非法；
- 重试创建新 Run，旧 Run 保持原终态；
- 取消与完成竞争只能产生一个终态；
- 进入 `CANCELLATION_REQUESTED` 或租约失效后，迟到文本、工具结果和完成状态均被拒绝；
- `GET /stream` 只订阅或补拉，不得创建 Run 或启动执行器；
- Node 进程内注册表只保存临时执行句柄，不是权威状态。

### 6.1 排队消息与活跃 Run 的分工

- `AiRun` 只表示一次已经获准执行的工作，不用多个 `QUEUED` Run 充当输入队列；活跃 Run 存在时，后续用户输入先以可审计的 `AiMessage` 排队状态落库。
- 每个 Thread 的排队顺序由数据库事务分配的单调序号决定，不能依赖浏览器时间、内存数组或事件到达顺序；相同幂等键只能得到同一条排队消息。
- 当前 Run 进入终态后，分发事务只能原子领取该 Thread 队首的一条普通排队消息，创建新的 `AiRun(QUEUED)` 并同步维护 `activeRunId`；任何时刻仍只能存在一个非终态 Run。
- 普通再次发送不打断当前回答，消息显示为“排队中”；刷新、重连和服务重启后仍可按持久化顺序恢复展示与领取。
- “调整方向”持久化为高优先级排队消息，并把当前 Run 置为带 `USER_REDIRECTED` 原因的取消请求。只有当前 Run 以唯一终态收敛后，定向消息才可被领取；租约与 fencing 必须拒绝取消后的迟到文本、工具结果和完成写入。
- 多次“调整方向”采用“最新定向请求覆盖全部尚未领取的旧用户输入”的已确认策略：此前普通和定向消息均保留审计记录并转为 `SUPERSEDED`，不再等待执行；只有最新定向请求保持待领取。该语义由用户显式选择“调整方向”触发，不依赖模型猜测消息是否相关。

---

## 7. 分步实施计划

每轮只能推进一个目标步骤中的一个最小增量。当前步骤没有通过门禁前，不进入下一步骤。

### 7.1 步骤 2.1：契约与状态规则

状态：✅ 已完成。2.2-C 只会以窄补丁补充排队投递与定向取消语义，不重新打开本步骤已冻结的 Thread/Run 基础状态规则。

已完成：

- [x] 定义 `AiMessage`、`AiRun`、基础 `AiEvent` 等共享类型；
- [x] 定义 Run 合法状态转换矩阵；
- [x] 实现非法状态转换的稳定领域错误；
- [x] 编写合法、非法和终态不可回退的表驱动测试；
- [x] 定义模型错误、Thread、Run、幂等、租约和事件冲突等稳定错误码。

边界修正：

- [x] 将 Thread 从“强制绑定 Project + Decision”改为只归属于用户且不保存任何业务上下文；
- [x] 将 `CreateAiThreadRequest` 收敛为首条消息和幂等键；
- [x] 删除 `entryProjectId`、`entryDecisionId`、`entryMeetingId`、Thread `ACTIVE/LOCKED` 和入口上下文错误码；
- [x] 删除随已撤销入口上下文设计产生、现已无业务意义的规则实现和单元测试；
- [x] 将 Run 的旧 `SCOPE_CHANGED` 取消原因改为来源级 `SOURCE_ACCESS_REVOKED`；
- [x] 明确业务目标由 Agent 从消息解析，歧义时追问，权限和来源依赖按每次工具调用与 Run/消息处理；
- [x] 跑通 contracts 类型检查和 Run 状态机测试；
- [x] 更新上位总路线图，声明第二阶段具体执行以本文为准并移除单 Decision 冲突。

完成门禁：

- [x] Thread 不保存业务上下文的方案经老大确认；
- [x] contracts 不再阻碍跨项目查询，也不要求 UI 选择项目、决策或会议；
- [x] Run 合法/非法状态转换测试确定性通过；
- [x] 稳定错误码和来源撤权取消原因完成冻结；
- [x] 本步骤完成后停止，等待确认进入 2.2-A。

### 7.2 步骤 2.2：持久化与事务地基

状态：🟡 部分完成（2.2-A/2.2-B 已完成；2.2-C 待开始）。

建议拆成三个可独立审查的增量；前两个已完成，第三个因新增的排队交互补入计划。

#### 2.2-A：Prisma 核心模型与 migration

- [x] 新增 `AiThread`、`AiMessage`、`AiRun`、`AiEvent`；
- [x] 只通过 Prisma 命令生成新 migration，不修改历史 migration；
- [x] 为 Thread 归属、消息分页、Run 状态、事件补拉建立必要索引；
- [x] 建立 `(runId, sequence)` 唯一约束；
- [x] 落实 `activeRunId` 快速指针和 PostgreSQL 非终态部分唯一索引的双重门禁；
- [x] 在隔离 PostgreSQL 中验证 migration 应用、`migrate reset` 后的重新应用和 schema 状态；
- [x] 未实现模型调用、HTTP 流和 UI。

#### 2.2-B：事务创建、幂等与事件追加

- [x] 原子创建 Thread + 首条用户消息 + 首个 Run；
- [x] 无活跃 Run 时原子创建后续用户消息 + Run；活跃 Run 下的后续输入在 2.2-C 改为持久化入队；
- [x] 定义创建 Thread、发送消息、重试三类幂等键作用域；
- [x] 相同幂等请求只返回同一资源，不重复创建；
- [x] 不同请求并发提交到同一 Thread 时只允许一个非终态 Run；
- [x] 事件序号通过数据库原子分配，未使用 `MAX(sequence) + 1`；
- [x] Run 终态和 Thread 活跃指针在同一事务中更新；
- [x] 增加真实 PostgreSQL 并发测试。

#### 2.2-C：持久化输入队列与定向请求地基

状态：✅ 已完成。本增量只补齐数据模型与事务分发不变量，不接模型、HTTP 流或 UI。

- [x] 以窄契约补丁定义用户消息投递状态、提交模式（普通排队/调整方向）和 `USER_REDIRECTED` 取消原因；不改变既有 Run 状态机的终态语义；
- [x] 通过 Prisma migration 为 `AiThread` 增加原子队列序号来源，并为 `AiMessage` 增加可审计的排队状态、队列序号和提交模式；为 Thread 内队列序号建立唯一约束；
- [x] 将“活跃 Run 期间创建后续用户消息 + Run”的直接事务改为“先持久化入队”；只有无活跃 Run 时才能原子领取队首并创建新的 `AiRun(QUEUED)`；
- [x] 定义普通消息的 FIFO、同一幂等键重放和并发入队的确定性结果；“调整方向”替代全部尚未领取的旧用户输入，旧普通与旧定向消息均保留 `SUPERSEDED` 审计状态，不物理删除；
- [x] 将 `QUEUED -> DISPATCHED` 与 `QUEUED -> SUPERSEDED` 固化为独立的 `AiMessage` 投递状态机；`DISPATCHED` 与 `SUPERSEDED` 均为无出口终态；
- [x] 保持 `activeRunId` 与非终态部分唯一索引作为单执行门禁，不将多个排队输入建模为并行 Run；
- [x] 编写真实 PostgreSQL 测试：活跃 Run 下普通发送仅新增排队消息、Run 终态后队首仅被领取一次、并发入队顺序稳定且不丢失、幂等重放不重复入队；
- [x] 未实现执行器取消、租约、HTTP API、输入框或模型调用；这些分别留给 2.3、2.5、2.6。

完成门禁：

- [x] migration 可应用，并可经测试数据库 reset 后重新应用；
- [x] 重复请求不产生重复 Thread、Message 或 Run；
- [x] 同一 Thread 不产生两个非终态 Run；
- [x] 事件序号唯一、单调且可按 `afterSequence` 补拉。

2.2-C 额外完成门禁：

- [x] 活跃 Run 下的新普通输入可持久化排队，不再以 `AI_THREAD_RUN_ACTIVE` 作为正常交互结果；
- [x] 任何时刻仍只有一个占用执行门禁的非终态 Run；
- [x] 队列顺序、定向替代和幂等重放均由数据库事务确定，并发测试不丢失、不重复、不越序；
- [x] 排队消息和被替代的定向请求均保留可审计状态。

### 7.3 步骤 2.3：执行租约、取消与故障收敛

状态：✅ 已完成。真实 PostgreSQL 并发与故障门禁已通过。

- [x] Run 只能通过原子 `QUEUED -> RUNNING` 被一个执行器领取；
- [x] 队列分发器只在 Thread 没有活跃 Run 时原子领取队首消息，并创建对应的 `AiRun(QUEUED)`；
- [x] 领取时签发 `executionLeaseId` 和过期时间；
- [x] 执行器定期续租；
- [x] 事件和终态写入校验 Run 状态与租约；助手消息和工具结果尚未在 2.3 前实现，其未来写入必须复用同一事务 fencing 服务；
- [x] 实现停止请求、取消确认和既有重试语义；
- [x] 实现“调整方向”：将当前 Run 以 `USER_REDIRECTED` 请求取消、使原租约失效并在唯一终态后优先分发已持久化的定向消息；不向已开始的模型请求直接注入文本；
- [x] 实现过期租约对账并释放 Thread 单 Run 门禁；
- [x] 编写普通队列/Run 终态竞争、取消/完成竞争、定向请求/完成竞争、失效租约迟到事件和进程异常的真实 PostgreSQL 用例；
- [x] 不实现跨进程续跑，异常 Run 由对账收敛为 `FAILED` 后仍沿用既有用户新建重试 Run 语义。

#### 2.3-A：原子领取与租约签发

状态：✅ 已完成。代码和真实 PostgreSQL 并发领取测试均已通过。

- [x] `AiRunService.claimQueuedRun` 使用单条带 `id + QUEUED` 条件的更新领取 Run；更新计数不是 1 时返回 `null`，调用方不得启动第二个执行器；
- [x] 成功领取时写入 `RUNNING`、`startedAt`、随机 `executionLeaseId` 和 30 秒后的 `executionLeaseExpiresAt`；
- [x] 新增两个并发领取请求竞争同一 Run 的真实 PostgreSQL 用例，断言只返回一份租约且数据库最终状态与该租约一致；
- [ ] 不实现续租、租约 fencing、终态写入校验、取消、对账、HTTP、模型调用或 UI。

#### 2.3-B：续租、fencing、取消与故障收敛

状态：✅ 已完成。实现和真实 PostgreSQL 事务测试均已通过。

- [x] `AiExecutionLeaseService` 只允许当前、未过期的 `RUNNING` 执行器续租；错误、失效、过期或已取消的租约统一返回 `AI_EXECUTION_LEASE_INVALID`；
- [x] 执行器事件追加与正常完成/失败终态在同一事务内验证租约；用户控制状态事件仅由已锁定 Thread 的状态事务写入，避免绕过执行器 fencing；
- [x] 停止 `QUEUED` Run 立即收敛为 `CANCELLED`；停止 `RUNNING/WAITING_APPROVAL` Run 则写入 `CANCELLATION_REQUESTED`、清空租约并等待取消确认或对账；
- [x] `STEER` 消息先持久化，再在同一 Thread 事务中以 `USER_REDIRECTED` 取消旧 Run；旧 Run 终态后再原子领取当前唯一未替代的定向消息；
- [x] 过期 `RUNNING` Run 收敛为 `FAILED(EXECUTION_LEASE_EXPIRED)`，过期取消请求收敛为 `CANCELLED`，两种路径都释放活跃指针并领取队首；
- [x] 不增加跨进程接管或续跑；对账只收敛遗留状态，后续执行通过新的用户重试 Run 开始。

完成门禁：

- 单 Run 只能领取一次；
- 队首消息只能分发一次，普通队列与定向请求不会生成并发 Run；
- 取消与完成竞争只有一个终态；
- 定向取消后旧 Run 的迟到写入全部被拒绝，且定向消息只会在旧 Run 收敛后优先续跑；
- 失效租约写入全部被拒绝；
- 对账能够确定性收敛遗留 Run 并允许后续重试。

### 7.4 步骤 2.4：真实单 Thread 运行闭环

状态：✅ 已完成。2.4-A～2.4-H 的功能增量、2.4-I～2.4-L 的收尾增量和 2.4-M 的评审修复均已通过定向验证与真实 PostgreSQL 回归。

本步骤穿插一条最小真实只读工具链：`findDecisionCandidates` + `getDecisionContext`。用户在消息中直接给出决策名称、别名或可见标识符，Agent 先发现候选；唯一命中后才读取上下文，重名或信息不足时展示受权限过滤后的候选并追问。明确 `decisionId` 只可作为测试 Fixture，不作为生产 UI 的必填输入。该链路可以同时验证“业务目标来自可见用户消息”“工具串联”和“工具在 NestJS 实时鉴权”；名称外的跨业务实体发现与非结构化内容检索不在本步骤展开。

- [x] 实现中心工具注册表和 Agent Runtime；
- [x] 实现 `findDecisionCandidates`：只按当前用户可访问范围查询名称、别名或可见标识符，返回受控数量的候选 DTO；不得通过候选数量、排序或错误信息泄漏无权 Decision 的存在（见 2.4-C）；
- [x] 实现 Agent 的最小串联规则：唯一候选才调用 `getDecisionContext`，多候选或无候选时追问；不得由模型猜测 ID 后继续读取（见 2.4-F）；
- [x] 实现 `getDecisionContext` 的窄输入、受控输出和来源标识（见 2.4-D）；
- [x] Web 只经 BFF/NestJS 获取业务数据，不直接访问 Prisma；
- [x] 持久化模型步骤、工具调用摘要、来源依赖、文本事件和最终消息（见 2.4-E）；
- [x] 实现首次流、`afterSequence` 补拉、停止和普通重试（见 2.4-H）；
- [x] 建立基础上下文预算，只注入当前请求和受限最近消息（见 2.4-E/2.4-H）；
- [x] 验证无权 Decision、私有区域和权限中途撤销：SQL 层数据范围、定向单测和真实 PostgreSQL“执行中撤销项目成员身份”用例均已覆盖；
- [x] 不扩展 proposal/vote/meeting/project 工具，不搜索讨论正文或会议转写，不接 RAG。

#### 2.4-A：Runtime 内部领取入口

状态：✅ 已完成。本增量只冻结 NestJS 与 Agent Runtime 的内部领取边界，不运行模型或读取业务数据。

- [x] 定义仅供内部执行链路使用的服务身份和 `runId` 输入；浏览器请求不得构造 Runtime 执行上下文；
- [x] Runtime 成功领取 Run 后，从持久化记录重新读取 Thread 所有者、原始用户消息、模型角色和执行租约，不接收外部传入的派生用户或业务范围；
- [x] 原子领取失败或租约在读取上下文前失效时返回 `null`，调用方不得启动执行器；
- [x] 新增不依赖 PostgreSQL、模型或工具的定向单元测试；
- [x] 内部 HTTP 服务鉴权、模型调用、工具注册表、Decision 查询、BFF、流和 UI 留给后续增量（已在 2.4-C～2.4-H 落地）。

#### 2.4-B：中心工具注册表边界

状态：✅ 已完成。本增量只定义只读工具的描述与调用上下文，不注册或执行真实工具。

- [x] 定义稳定工具名称、只读访问模式、窄输入/输出字段、超时和从已领取 Run 派生的当前用户执行上下文；
- [x] 实现中心注册表：启动时拒绝重复名称、写能力、空字段说明和非法字段类型，未注册名称无法被解析；
- [x] 对已注册描述深拷贝冻结，避免模块外修改原始配置后改变 Runtime 可用工具；
- [x] 新增不依赖 PostgreSQL、模型或业务工具的定向单元测试；
- [x] 2.4-F 在描述上追加了可选的 `discoveryRequirement`（前置发现声明），注册表同步校验该声明指向的字段真实存在、不指向自身，并一并深拷贝冻结。

#### 2.4-C：`findDecisionCandidates` 受权限保护查询与候选 DTO

状态：✅ 已完成并重新验证。

- [x] 定义 `findDecisionCandidates` 工具描述并注册进中心工具注册表：输入仅 `query`，输出为受控数量的候选摘要数组；描述抽到 `ai/tools/decision/find-decision-candidates.descriptor.ts`，与执行器解耦；
- [x] 新增 `AiPermissionPolicyService`：为 Runtime 内部执行上下文的 `ownerUserId` 现取现算与登录态一致的授权上下文；用户不存在或状态非 ACTIVE 时拒绝；
- [x] **架构调整**：真实 Decision 查询下沉到 `decisions` 模块的 `DecisionDiscoveryService.findCandidates`，权限范围复用 `AuthorizationService.buildDecisionWhere('decision:read')`，SQL 层排除未授权 Decision；ai 模块不再直接对 `Decision` 表发起 Prisma 查询；
- [x] ai 模块侧是薄执行器 `FindDecisionCandidatesToolService`：只做输入校验、派生权限上下文、委托查询、映射输出契约；
- [x] 工具文件按业务域拆分目录：`ai/tools/decision/` 下放 descriptor、执行器和类型定义；后续新工具照此模式各开一个 `tools/<domain>/` 子目录；
- [x] 名称匹配范围：按 `title` 大小写不敏感模糊匹配，纯数字查询词额外按主键精确匹配；候选上限 5 条，`DecisionDiscoveryService` 自身再做 1～20 条防御性夹紧；
- [x] **2.4-F 引入的变化**：执行器统一实现 `AiToolExecutor`，返回 `{ output, sources }`；候选决策会作为 `READ` 来源登记到 `AiSourceDependency`，对应单测已同步更新。

#### 2.4-D：`getDecisionContext` 受权限保护的上下文读取

状态：✅ 已完成并验证。

- [x] `decisions` 模块新增 `DecisionContextService.getContext(authorization, decisionId)`：复用 `buildDecisionWhere('decision:read')`，主键条件叠加在授权范围之上；决策不存在与无权访问统一返回 `null`，调用方无法区分两者；
- [x] 只读取决策自身的结构化事实与关联计数（标题、描述、状态、项目、分区、牵头部门、负责人/创建人展示名、参与人数、提案/投票/决议计数、最新一条生效决议摘要和关键时间点），不读取讨论正文、会议转写或提案投票明细；
- [x] ai 模块新增 `ai/tools/decision/get-decision-context.{descriptor,types,service}.ts` 薄执行器：校验主键为正整数、派生权限上下文、委托查询、截断描述（上限 500 字符）并映射为 ISO 时间字符串；
- [x] 声明本次调用实际读取的来源标识：决策本体登记为 `DECISION`，存在生效决议时额外登记 `DECISION_RESOLUTION`；
- [x] 未授权时抛出与“决策不存在”完全一致的 `DECISION.NOT_FOUND`，不通过错误码或文案泄漏存在性；
- [x] 新增 `decision-context.service.spec.ts` 与 `get-decision-context.service.spec.ts` 定向单测，均 mock Prisma 与授权服务，不连真实数据库。

#### 2.4-E：模型步骤、工具调用与来源依赖持久化

状态：✅ 已完成；migration 已应用并通过真实 PostgreSQL 回归。

- [x] 按第 8 节草案新增 `AiStep`、`AiToolCall`、`AiSourceDependency` 三张表与 `AiToolCallStatus`、`AiSourceType`、`AiSourceUsage` 枚举，并为 `AiEventType` 追加 `TOOL_CALL_STARTED`、`TOOL_CALL_SETTLED`；
- [x] 生成 migration `20260825140000_add_ai_step_tool_call_source`：`(runId, sequence)`、`(runId, providerToolCallId)`、`(runId, sourceType, sourceId, usage)` 唯一约束，工具调用状态与结果字段的 CHECK 约束，来源可按 `(sourceType, sourceId)` 反查；
- [x] `AiStepService`：步骤序号由持有租约的唯一执行器按顺序递增，数据库唯一约束兜底，重复写入视为幂等重放；
- [x] `AiToolCallService`：发起与结束都在同一事务内校验执行租约，并同步追加 `TOOL_CALL_STARTED`／`TOOL_CALL_SETTLED` 事件；提供按声明字段提取历史候选标识的查询，供串联规则使用；
- [x] `AiSourceDependencyService`：按 `skipDuplicates` 登记 `READ` 来源，同一 Run 重复读取同一来源只保留一条；
- [x] `AiAssistantMessageService`：首段文本增量前创建助手消息占位，最终正文由 `completeRun` 在终态同一事务写入，避免出现“消息已完成但 Run 失败”的中间态；
- [x] `AiContextBudgetService`：基础上下文预算（最近 12 条、单条 2000 字符、合计 16000 字符，超出从最旧开始丢弃），只注入已分发用户消息与非空助手消息，排除当前 Run 自己的消息；
- [x] 共享契约同步扩展：`ai-tool.types.ts` 定义工具调用与来源展示契约，`ai-event.types.ts` 增加两类工具事件，`API_ERROR_CODES` 增加 5 个 AI 稳定错误码。

#### 2.4-F：工具调用编排与 Agent 最小串联规则

状态：✅ 已完成并验证。

- [x] 新增 `AiToolInvocationService` 作为唯一工具编排入口：解析已注册描述 → 校验前置发现规则 → 超时保护下执行 → 落库结果摘要与来源 → 返回可交回模型的结果；
- [x] 串联规则以**声明式**表达：需要“目标先被唯一发现”的工具在描述里写 `discoveryRequirement`（前置发现工具名、目标输入字段、候选数组字段、候选标识字段），编排层统一强制，新增同类工具不需要改编排代码；
- [x] 规则判定只依赖已持久化的工具调用记录：只有本 Run 内某次 `findDecisionCandidates` **恰好返回一个候选**且该候选主键与请求一致时才允许读取上下文；无发现记录、候选为空、候选多于一个或主键不一致都拒绝，并把“先发现、候选不唯一请向用户确认”的指引作为工具错误交回模型；
- [x] 未注册工具名称一律拒绝，但调用尝试同样落库，保证审计完整；
- [x] 工具异常归一化：`BusinessException` 保留稳定错误码与中文说明，未知异常只返回脱敏说明，不把内部错误暴露给模型；
- [x] 执行器通过 `AI_TOOL_EXECUTORS` 工厂注入，启动时拒绝重复名称和没有对应注册描述的执行器；
- [x] 新增 `ai-tool-invocation.service.spec.ts`（8 条）覆盖未注册工具、无发现记录、多候选、主键不匹配、唯一命中、业务异常、未知异常和执行器注册校验。

#### 2.4-G：内部执行接口与浏览器 Run 接口

状态：✅ 已完成并验证。

- [x] 内部鉴权按老大确认采用共享密钥：新增 `AI_RUNTIME_SERVICE_TOKEN` 环境变量（生产必填、至少 32 位），`AiRuntimeServiceGuard` 用定长比较校验 `x-ai-runtime-token` 请求头，**未配置密钥时一律拒绝（fail closed）**；两个 `.env.example` 已同步；
- [x] 新增内部执行控制器 `internal/ai/runs/*`：领取、续租、助手消息占位、文本增量、模型步骤、工具调用、终态收敛、取消确认；控制器标记 `@Public()` 跳过登录守卫，只由服务密钥保护，并从 Swagger 中排除；
- [x] 新增 `AiRuntimeSessionService` 作为内部执行编排门面：所有用户身份、业务范围和工具执行上下文都从持久化 Run 现取现算，不接受调用方传入；
- [x] 新增浏览器接口：`POST /ai/threads`、`POST /ai/threads/:threadId/messages`、`GET /ai/runs/:runId/events`、`POST /ai/runs/:runId/stop`、`POST /ai/runs/:runId/retry`，全部要求 `ai:chat:use` 权限；
- [x] `AiRunQueryService` 按 Thread 所有者过滤事件查询，越权统一返回“不存在或无权访问”，单页最多 200 条并返回 `hasMore` 与 Run 状态快照；
- [x] 新增 `ai-runtime-service.guard.spec.ts` 覆盖未配置密钥、缺少请求头、密钥不匹配、长度不同和完全一致五种情况；
- [x] 说明：`POST /ai/threads/:threadId/messages` 原本属于 2.5 的接口清单，本轮提前落地，因为多轮对话是验证上下文预算和“排队/调整方向”的前提；Thread 列表、详情、消息分页、重命名归档仍留给 2.5。

#### 2.4-H：Web Agent Runtime 与 BFF/SSE

状态：✅ 已完成并验证。

- [x] `ai-runtime-client.server.ts`：Runtime 调用 NestJS 内部接口的唯一客户端，统一附加共享密钥并把统一失败响应转成可判断的运行时错误；
- [x] `ai-agent-tools.server.ts`：按 NestJS 返回的工具描述动态生成 AI SDK 工具，Runtime 不自定义任何工具能力，执行一律回到 NestJS 做实时鉴权与落库；工具失败以结构化结果交回模型，驱动追问而不是中断整轮；
- [x] `ai-agent-runtime.server.ts`：领取 → 组装受限上下文 → `streamText` 模型循环（最多 6 步）→ 文本增量按 120 字符/400ms 批量落库 → 记录模型步骤 → 写入最终消息与用量并收敛终态；
- [x] 每 10 秒续租一次；续租失败即中止模型流并确认取消，用户停止后失效租约可以在一个续租周期内停下执行器；
- [x] 终态事务领取到下一条排队消息时，Runtime 自动接着执行下一个 Run，“调整方向”和排队消息因此可以连续跑通；
- [x] `decision-hub-agent.ts`：系统指令明确“先发现、唯一候选才读取、候选为空或多个必须追问、只基于工具数据回答、当前只读”；
- [x] `ai-stream.server.ts` + `GET /api/ai/threads/:threadId/stream`：SSE 只按事件序号补拉已落库事件，**绝不启动执行器**，因此刷新、重连和多标签页订阅都不会产生第二个执行器；浏览器断开只结束订阅，不取消运行；
- [x] BFF 路由 `POST /api/ai/threads`、`POST /api/ai/threads/:threadId/messages`、`POST /api/ai/runs/:runId/stop`、`POST /api/ai/runs/:runId/retry` 复用现有认证代理，并在响应返回后用 `after()` 启动执行器；领取是原子的，重复启动不会并发执行；
- [x] 未改动任何 AI 工作台 UI 组件；真实数据接入与四态处理仍按计划留给 2.6。

#### 2.4-I：Runtime 生命周期托管与故障恢复收尾

状态：✅ 已完成并验证。

- [x] `after()` 返回可等待的执行 Promise，避免请求生命周期中的执行链被无监督丢弃；
- [x] 后继 Run 使用独立串行调度器持续认领，新增回归测试验证顺序和认领失败停止语义；
- [x] Next.js Node Runtime 通过 `instrumentation.ts` 启动 10 秒一次的过期租约对账；多实例重复触发仍由数据库原子领取兜底；
- [x] 对账事务返回已领取的后继 Run，维护器持有恢复任务但不阻塞下一轮扫描。

#### 2.4-J：步骤、工具调用和终态审计一致性收尾

状态：✅ 已完成并验证。

- [x] 模型步骤通过供应商工具调用标识关联 `AiToolCall.stepId`；
- [x] 同一幂等键重放会校验工具名称与稳定输入指纹，已完成结果直接重放，不重复执行工具；
- [x] Run 完成、失败或取消时，同一终态事务会把遗留 `RUNNING` 工具调用收敛为 `FAILED` 并追加结算事件；
- [x] 工具描述与执行器执行双向启动校验，避免只声明未实现或只实现未声明。

#### 2.4-K：跨端契约、实时权限与 SSE 边界收尾

状态：✅ 已完成并验证。

- [x] Runtime session、工具描述、调用结果、对账结果和事件分页统一迁移到 `@workspace/contracts`；
- [x] 每次工具调用重新校验 `ai:chat:use`，Decision 发现与读取在 SQL 范围外再显式校验 `decision:read`；
- [x] 真实 PostgreSQL 用例确认执行中撤销项目成员身份后，后续上下文工具调用被拒绝；
- [x] SSE 从路径传入并在 NestJS 查询中同时校验 `threadId`、`runId` 和所有者，避免跨 Thread 补拉。

#### 2.4-M：2.4 代码评审问题修复

状态：✅ 已完成并通过真实 PostgreSQL 回归。针对 2.4-I～2.4-L 评审发现的 5 个问题：

- [x] **孤儿排队 Run 恢复**：过期租约扫描只覆盖 `RUNNING`/`CANCELLATION_REQUESTED`，而 `QUEUED` Run 没有租约（`executionLeaseExpiresAt` 为 NULL，SQL 比较不成立），一旦调度回调随进程重启丢失，该 Run 会永久占住 `activeRunId` 把整条 Thread 堵死。对账新增孤儿分支：超过 30 秒宽限仍未被领取的 `QUEUED` Run 交回 `nextRunIds` 重新派发，不改状态，领取仍由执行器原子完成；
- [x] **事件补拉参数校验**：`threadId` 原为裸查询参数，缺失时 Prisma 会把 `undefined` 当作“未提供”静默丢弃该条件，2.4-K 声称的三重校验对直接调用不成立。改为 `ListAiRunEventsQueryDto`，`threadId` 必填、`afterSequence` 同步补齐范围校验；
- [x] **截断摘要不得重放**：`REPLAY_SUCCEEDED` 直接返回落库 `outputSummary`，但超长输出落库时只保留 `{ truncated: true, length }` 标记，会把截断标记当成成功结果交回模型。截断约定抽到 `ai-tool-output-summary.ts`，重放方识别到截断标记时改判 `REPLAY_UNAVAILABLE`，返回引导模型重新调用的稳定失败；
- [x] **重放路径补齐租约 fencing**：`findExistingToolCall` 原本用裸 Prisma 读取且不校验租约，租约已失效的旧执行器仍能把既有工具结果读回模型。改为在事务内先 `assertActiveExecutionLease`，与 2.4-J 的步骤重放路径保持同一套标准；
- [x] **维护循环清理**：删除只写不读的 `activeExecutions` 死状态；密钥门禁统一为与 NestJS 一致的 ≥32 位判断（原本只判非空，短密钥会导致每 10 秒报错一次且永不停止）；连续失败改为抽样记录，避免只跑 web 时日志被刷满；
- [x] 附带修复：`test` 脚本补上 `--experimental-vm-modules`，`ai-evaluation.spec.ts` 不再让默认测试命令整体失败；补齐 AI 模块遗留的 ESLint/Prettier 问题。

#### 2.4-L：测试隔离、临时资产与计划文档收尾

状态：✅ 已完成。

- [x] Jest 不再全局加载开发 `.env`，真实 PostgreSQL 用例继续要求显式测试连接变量；
- [x] 删除 `_to_delete` 中已经确认无引用的旧实现副本、路由副本和锁文件，删除不再使用的 Jest 环境脚本；
- [x] 完成定向单测、状态机、真实 PostgreSQL、Web Runtime、contracts/Web 类型检查与修改文件 ESLint；
- [x] 本文同步记录当前实现、验证结果、后续拆分和变更记录。

已执行的核心验证命令：

```bash
# 1) 应用本轮 migration（会新建三张表并扩展 AiEventType 枚举）
pnpm --filter @nextnest/server exec prisma migrate deploy
pnpm --filter @nextnest/server exec prisma generate

# 2) 定向单测（不连真实数据库）
pnpm --filter @nextnest/server test -- --runInBand modules/ai/policies/ai-permission-policy.spec.ts modules/ai/tools/decision/find-decision-candidates.service.spec.ts modules/ai/tools/decision/get-decision-context.service.spec.ts modules/ai/services/ai-tool-registry.service.spec.ts modules/ai/services/ai-tool-invocation.service.spec.ts modules/ai/services/ai-context-budget.service.spec.ts modules/ai/guards/ai-runtime-service.guard.spec.ts modules/decisions/services/decision-discovery.service.spec.ts modules/decisions/services/decision-context.service.spec.ts

# 3) 既有状态机回归
pnpm --filter @nextnest/server test -- --runInBand modules/ai/state/ai-run.machine.spec.ts modules/ai/state/ai-message.machine.spec.ts

# 3b) 真实 PostgreSQL 回归。集成测试的清理钩子会无条件清空 AI 相关表，
#     必须使用隔离测试库；未配置 AI_PERSISTENCE_TEST_DATABASE_URL 时整个 Suite 会被 skip。
#     一次性建库（复用 DATABASE_URL 的连接信息，只换库名，已存在则跳过）：
pnpm --filter @nextnest/server test:db:create
#     再在 DATABASE_URL 指向该测试库的前提下同步表结构：
#       pnpm --filter @nextnest/server exec prisma migrate deploy
#     之后把该库连接串写进 apps/server/.env 的 AI_PERSISTENCE_TEST_DATABASE_URL，
#     集成用例即随普通测试命令一起运行。

# 4) 类型检查
pnpm --filter @workspace/contracts check-types
pnpm --filter @nextnest/server exec tsc --noEmit
pnpm --filter @nextnest/web exec tsc --noEmit
```

部署运行时需要在 `apps/server/.env` 与 `apps/web/.env.local` 配置同一个 `AI_RUNTIME_SERVICE_TOKEN`（至少 32 位随机字符），否则内部执行接口会按 fail closed 策略全部拒绝。

完成门禁：

- [x] 模型工具循环测试能够完成“工具调用 → 后续回答”，工具编排定向测试覆盖“发现 → 唯一候选 → 读取上下文”；
- [x] SSE 只补拉持久化事件，刷新或重连不会启动执行器；
- [x] 重名、模糊名称或无结果只会得到受权限过滤的候选或追问，Agent 不会猜测 ID 并读取任意 Decision；
- [x] 越权请求由 NestJS 拒绝，真实 PostgreSQL 撤权用例确认不会继续读取或登记来源内容。

### 7.5 步骤 2.5：会话历史、来源权限与 BFF 接口

状态：✅ 已完成。全部增量见下表。

| 增量 | 内容 | 状态 |
| --- | --- | --- |
| 2.5-A-1 | Thread 列表/详情契约与游标规则 | ✅ 已完成 |
| 2.5-A-2 | Thread 列表/详情 NestJS 只读实现 | ✅ 已完成 |
| 2.5-P-1 | 置顶数据模型与契约 | ✅ 已完成 |
| 2.5-P-2 | 置顶列表查询与置顶/取消写接口 | ✅ 已完成 |
| 2.5-A-3 | Message 游标分页契约与实现（含 Run、工具调用展示数据） | ✅ 已完成 |
| 2.5-B | 会话重命名、归档、恢复及归档清除置顶的联动 | ✅ 已完成 |
| 2.5-C-1 | 来源失权判定逻辑（实时、批量、fail closed） | ✅ 已完成 |
| 2.5-C-2 | 把判定投影到消息与 Run 输出（中性占位） | ✅ 已完成 |
| 2.5-D | Next.js BFF 路由与旁路越权回归测试 | ✅ 已完成 |

- [x] Thread 游标分页列表和详情（2.5-A-1 契约、2.5-A-2 实现）；
- [x] Message 游标分页及 Run、工具调用展示数据；**引用展示数据无法在本阶段交付**：`AiCitation` 表尚未建立（第 8 节数据模型表标注为“2.4/后续阶段”，2.4 实际未落地），真实引用生成也不在第二阶段范围内，因此 2.5 只交付消息、Run 与工具调用，引用位保持真实空状态；
- [x] `POST /messages` 接收幂等键与提交模式，返回投递状态、队列位置和已关联 Run（`AiThreadMessageSubmissionResult` 含 `dispatchState`/`queueSequence`/`runId`），已随 2.2-C、2.3 落地；
- [x] Thread 重命名、归档和恢复；
- [x] Run 实际读取的 `READ` 来源依赖已在 2.4 登记；2.5 只消费该关系实施历史权限投影，`CITED` 留给后续真实引用生成；
- [x] 来源失权后的单 Run/消息级锁定或隐藏；
- [x] 对应 Next.js BFF 路由；
- [x] 列表、直接 URL、消息、工具结果和事件的旁路权限测试；引用入口本阶段不存在，未伪造占位测试；
- [x] **置顶已由老大确认纳入 2.5 范围**（原计划排除），见 2.5-P；
- [ ] 不实现物理删除、会话搜索、文件夹和共享。

#### 2.5-A-1：Thread 列表/详情共享契约与游标规则

状态：✅ 已完成。本步骤只冻结跨端类型，不含任何查询实现、接口或 UI。

- [x] 新增 `packages/contracts/src/ai/ai-thread-history.types.ts`，导出 `AiThreadListQuery`、`AiThreadListItem`、`AiThreadPage`、`AiThreadActiveRun`、`AiThreadDetail`、`AiThreadListFilter` 与分页上限常量；
- [x] **游标规则**：列表按 `updatedAt` 倒序；`cursor` 为服务端生成的不透明字符串，内部同时承载排序时间与 Thread 标识，保证同一时间戳下顺序稳定、不重复不漏项；客户端只能原样回传，不得自行构造或解析；切换 `filter` 必须丢弃旧游标重新从首页开始；
- [x] **所有者语义**：数据范围恒为“当前登录用户即所有者”，因此列表项不再返回 `ownerUserId`（恒等于调用方，返回即冗余）；越权统一表现为“不存在”，不区分无权与不存在；
- [x] `AiThreadActiveRun.status` 收窄为 `AiRunNonTerminalStatus`：Run 收敛为终态时 `activeRunId` 会在同一事务清空，详情此时返回 `activeRun: null`，由类型层面排除“返回终态活跃 Run”的实现分支；
- [x] 详情不内联消息，避免响应随历史增长无上限膨胀；消息由后续独立分页接口提供。

验证：`@workspace/contracts`、`@nextnest/web`、`@nextnest/server` 三个包 `tsc --noEmit` 通过；确认新导出与既有 barrel 无重名冲突。本步骤为纯类型冻结，contracts 不允许承载运行时实现，因此没有可运行的契约级单测；游标编解码属于服务端实现，随 2.5-A-2 一并补测试。

**后续修正**：老大已确认置顶纳入 2.5 范围，本契约随 2.5-P 补上 `pinnedAt` 与固定列表，原“留给 2.6 决策”的缺口已关闭。

#### 2.5-A-2：Thread 列表/详情的 NestJS 只读实现

状态：✅ 已完成。只做只读查询，不含消息分页、置顶写接口、BFF 和 UI。

- [x] `ai-thread-cursor.ts` 实现不透明游标编解码：`base64url(JSON)`，承载版本、筛选条件、排序时间与 Thread 标识。**明确做的是结构校验而不是防篡改签名**——数据范围由 SQL 中的所有者条件独立保证，伪造游标最多只能改变自己列表的起始位置，读不到他人数据，因此不引入 HMAC 成本；非法游标返回 `AI.THREAD_CURSOR_INVALID`，不静默降级为“从头开始”；
- [x] 游标编码筛选条件并在解码时比对：切换 `filter` 复用旧游标从“行为未定义”改为**稳定报错**，比让客户端拿到静默错乱的分页更安全（同步修正 2.5-A-1 契约注释）；
- [x] `AiThreadQueryService.listThreads`：`ownerUserId` 与 `pinnedAt: null` 恒定生效，归档筛选只改变归档条件；`orderBy [updatedAt desc, id desc]` 配合 keyset 条件 `updatedAt < ? OR (updatedAt = ? AND id < ?)`；多取一条判断 `hasMore`，游标指向截断后的最后一条；
- [x] `getThreadDetail`：不存在与非所有者归一化为同一个 `AI.THREAD_NOT_FOUND`；活跃 Run 用既有 `isAiRunNonTerminalStatus` 类型守卫收窄，指针指向终态时按无活跃 Run 返回，不需要类型断言；
- [x] `ListAiThreadsQueryDto` 与 `GET /ai/threads`、`GET /ai/threads/:threadId`，注册进 `ai.module.ts`；
- [x] `pinnedAt` 加入列表与详情投影，Prisma Client 已重新生成。

验证：游标单测 5 条、查询服务单测 10 条；真实 PostgreSQL 新增 2 条——**同一 `updatedAt` 的 5 条会话按 limit=2 翻页不重复不漏项且排除已固定会话**（这是只按时间做游标时必然失败的场景），以及详情的所有者边界与活跃 Run 快照。AI 模块 15 个 Suite、152 条测试通过；类型检查与 ESLint 通过。

#### 2.5-C-1：来源失权判定逻辑

状态：✅ 已完成。本轮只做判定，不改动任何对外响应结构。

**D2-07 已确认为方案 A：实时判定，权限恢复后历史自动重新可见。** 不持久化任何“已锁定”标记——AI 历史本质是“当时读到了什么”的审计记录，权限恢复了就应当恢复可见；持久化锁定状态没有明确的解锁责任人，容易变成永远堆积的死数据。

- [x] `DecisionVisibilityService`（decisions 模块）批量筛出仍可见的决策与决议。**复用 `buildDecisionWhere('decision:read')`**，主键条件叠加在授权范围之上而不是替换它，避免历史投影与真实权限规则漂移出现“详情看不到但历史仍显示”；决议没有独立数据范围，可见性等同其所属决策；
- [x] `AiSourceVisibilityService`（ai 模块）按 Run 批量判定：一次取出全部来源依赖，**按来源类型各发一次查询**，不随消息条数退化成 N+1；没有登记来源的 Run 视为可见；
- [x] **fail closed**：未知来源类型、无法解析为主键的 `sourceId` 一律判定为不可见。将来新增来源类型却忘了补判定时，结果是历史被过度隐藏而不是越权泄漏；
- [x] 判定按 2.4 已登记的 `READ` 关系计算，`CITED` 留给后续真实引用生成；
- [x] 分层沿用 2.4-C 确立的模式：决策查询下沉 decisions 模块，ai 模块只做来源聚合与编排。

验证：真实 PostgreSQL 新增 1 条，覆盖完整生命周期——有权限可见 → 撤销项目成员身份后该 Run 判定为不可见（无来源依赖的 Run 不受影响）→ **恢复成员身份后自动重新可见，无需任何显式重评** → 无法解析的来源标识按 fail closed 判定不可见。用例同时断言可见与不可见两个方向，不会因为判定恒返回真而空过。AI 与 decisions 模块 19 个 Suite、212 条通过。

#### 2.5-D：BFF 路由与旁路越权回归

状态：✅ 已完成，2.5 收尾。

- [x] 补齐 Next.js BFF 路由：会话列表（GET）、详情、消息历史（GET）、固定列表、固定/取消、重命名、归档恢复，全部复用既有认证代理；
- [x] 代理支持的方法补上 `PUT`（原先只有 `GET/POST/PATCH/DELETE`），与服务端幂等的整体状态设置语义一致；
- [x] **BFF 不做任何权限判断也不改写响应**：数据范围与失权投影全部由 NestJS 强制。转发层再写一套规则必然与服务端漂移，是这类架构最常见的越权来源；
- [x] `/api/ai/threads/pinned` 是静态段，Next.js 优先于 `[threadId]` 动态段匹配，因此不会被当作 `threadId` 为 `pinned` 的详情请求——与服务端控制器的路由顺序约束同理，已在文件注释中写明；
- [x] 清理 `_placeholder.ts`：删除已被真实路由取代的 threads、messages、stream 三个占位；删除 `[threadId]/stop` 整个目录——停止是 Run 级操作，真实路由在 `/api/ai/runs/[runId]/stop`，该占位指向一个不会存在的接口；保留 approvals 占位（属于第 4 阶段）。

**旁路越权回归**：新增一条真实 PostgreSQL 用例，用两个真实用户覆盖四类入口——列表只返回自己的会话；直接拿他人 Thread 标识查详情返回“不存在”而非“无权”；消息历史（工具调用摘要的唯一出口）同样被会话归属挡住；事件补拉在“换用户”和“正确 runId 配错误 threadId”两种组合下都拒绝，同时断言所有者按正确组合仍能正常补拉，避免规则收得过紧却无人发现。引用入口本阶段不存在，不做占位测试。

验证：AI 与 decisions 模块 19 个 Suite、214 条通过（真实 PostgreSQL 30 条）；Web 与 contracts 类型检查、ESLint 通过。

#### 2.5-C-2：把失权判定投影到消息输出

状态：✅ 已完成。

- [x] 契约新增 `contentVisibility`（`VISIBLE` / `SOURCE_REVOKED`）。**必须在类型层明确表达"被隐藏"**，否则客户端只看到空正文会渲染成"AI 没有回答"，把权限问题误报成功能故障；
- [x] 失权时服务端清空 `content` **并清空 `run.toolCalls`**：工具名称与数量同样能反推信息（例如"这里调用过 getDecisionContext"等于告诉用户存在一个他无权看到的决策）。最小披露原则，不留可推断痕迹；
- [x] **保留 Run 的状态、失败原因与取消原因**：这些描述的是用户自己这次运行的结果，不承载任何来源内容，隐藏它们反而让用户无法理解历史；
- [x] **用户消息永不隐藏**：用户自己写的内容不依赖他人授权。数据结构上也天然如此——`runId` 只挂在助手消息上；
- [x] **整页一次性判定**：在组装响应前批量调用，不在映射循环里逐条查询；
- [x] `listMessages` 改为接收 `AuthorizationContext` 而非裸 `ownerUserId`：失权判定需要完整授权上下文才能复用决策数据范围。

**占位文案的红线**（写给后续 UI 实现）：`AiSourceDependency.label` 存了来源名称快照，当初是为"说明受影响范围"设计的，但**绝不能**用它渲染成"因《某某决策》失权而隐藏"——那本身就是把用户无权看到的标题告诉了他。占位不得包含来源标题、摘要、名称，也不得暴露数量。

验证：真实 PostgreSQL 新增 1 条，走完整生命周期——有权限时正文与工具调用正常 → 撤权后 `contentVisibility` 转为 `SOURCE_REVOKED`、正文清空、工具调用清空 → **整页序列化后断言不含助手正文、决策标题与工具名** → 用户消息仍为 `VISIBLE` 且正文完整 → 权限恢复后正文与工具调用实时回来。AI 与 decisions 模块 19 个 Suite、213 条通过。

#### 2.5-B：会话重命名、归档与恢复

状态：✅ 已完成。

- [x] **确立 `updatedAt` 不变式并写进契约**：只有会话活动（新消息、Run 状态变化）推进 `updatedAt`；重命名、固定、归档、恢复等元数据变更一律保持原值。否则改个标题就会让几个月前的会话跳到“最近”列表最前面；
- [x] **归档要求没有活跃 Run**（老大确认）：存在活跃 Run 时返回既有的 `AI.THREAD_RUN_ACTIVE`，由用户先显式停止，而不是让归档隐式产生“取消正在生成的回答”的副作用。判断与写入在同一事务内，并复用 `AiQueueService.lockThread` 这把 Thread 行锁——与创建消息走同一把锁，避免“检查时空闲、写入时已产生新 Run”的竞态；
- [x] 归档同时清除 `pinnedAt`，与 2.5-P-2 的互斥规则一致；恢复不会自动重新固定，由用户决定；
- [x] 重命名去除两端空白并压缩内部连续空白，去除后为空则拒绝；超长按 `AI_THREAD_TITLE_MAX_LENGTH` 截断；
- [x] 归档、恢复、重命名重复设置为同一状态均幂等；非所有者统一返回 `AI.THREAD_NOT_FOUND`；
- [x] `PUT /ai/threads/:threadId/title` 与 `PUT /ai/threads/:threadId/archived`。

**顺带闭合一处状态矛盾**：归档要求没有活跃 Run，但原先仍可向已归档会话发送消息，从而产生“已归档 + 有活跃 Run”的矛盾状态。已在 `createMessageWithRun` 的同一把锁内拒绝，返回 `AI.THREAD_ARCHIVED`；`lockThread` 相应扩展返回 `archivedAt`。这是归档语义的必要推论，若老大更希望“发送时自动恢复”，改动点集中在这一处。

验证：真实 PostgreSQL 新增 3 条（元数据变更不推进时间且归档清除固定、活跃 Run 拒绝归档与归档后拒收消息、所有者边界与空标题）。AI 模块 15 个 Suite、163 条通过。

> 测试编写记录：首版用例把基准 `updatedAt` 设在 `requestStop` 之前，断言失败。核对后确认**是断言写错而非实现有误**——停止 Run 属于 Run 状态变化，本就应当推进 `updatedAt`。调整为在 Run 进入终态之后再设定基准值，测的才是“元数据变更是否推进时间”。

#### 2.5-A-3：Message 游标分页

状态：✅ 已完成。

- [x] **返回顺序在契约层固定**：`items` 按创建时间**正序**返回可直接渲染，而游标向更早方向推进，`nextCursor` 指向本页**最早**一条。顺序写死在契约里，避免各客户端各自实现反转导致不一致；
- [x] **不返回工具输入与输出**：现有工具卡 `ai-tool-call-card.tsx` 只渲染工具名与状态，本就不需要输入输出；而 `outputSummary` 承载真实业务事实（如决策上下文），一旦来源失权，历史接口就会变成泄漏面。因此本轮只返回 `toolName`/`status`/`durationMs`/失败信息，**从源头上不制造需要 2.5-C 去补救的泄漏**。已用真实 PostgreSQL 用例断言输入输出不出现在响应中；
- [x] 助手消息带出所属 Run 的 `status`、失败原因与取消原因，满足“失败和取消不会被显示为完成”的阶段门禁；用户消息 `run` 为 `null`；
- [x] 被替代的历史用户输入照常返回，投递状态由 `dispatchState` 表达，由客户端决定呈现方式，不在服务端擅自隐藏用户提交过的内容；
- [x] 引用位保持真实空状态，不返回占位或伪造数据；
- [x] **游标编解码抽为通用 `ai-cursor.ts`**：新增 `kind` 与 `scope` 判别，会话列表游标不能拿去翻消息列表，消息游标绑定所属会话不能跨会话复用；错误码由 `AI.THREAD_CURSOR_INVALID` 泛化为 `AI.CURSOR_INVALID`；
- [x] `GET /ai/threads/:threadId/messages`，先校验会话归属再查询，越权统一返回 `AI.THREAD_NOT_FOUND`。

**顺带修复测试基建缺陷**：集成测试清理钩子按 `Run → Message` 顺序删除，而删除 Run 会把 `AiMessage.runId` 置为 NULL，违反 `AiMessage_role_origin_check`（助手消息要求 `runId` 非空）。这是既有潜伏问题，本轮首次留下助手消息才触发。已改为“先删助手消息 → 再删 Run → 最后删用户消息”，并把清理抽成函数在 `beforeAll` 也调用一次，使测试套件在上一轮中断后能自愈，不会被残留数据永久污染。

**记录一处模型张力**：`AiMessage.runId` 的外键动作是 `SetNull`，但 CHECK 约束禁止助手消息的 `runId` 为空——两者组合意味着**只要助手消息还在，对应的 Run 就删不掉**。当前产品没有物理删除路径（2.5 明确不实现），因此只是潜伏问题；将来若要支持删除会话或运行，需要先解决这个冲突。

验证：真实 PostgreSQL 新增 3 条（翻页方向与正序拼接、Run/工具展示数据且不泄漏输入输出、所有者与跨会话游标边界），游标单测扩到 7 条。AI 模块 15 个 Suite、160 条通过。

#### 2.5-P：会话置顶（数据模型与契约）

状态：✅ 数据模型与契约已完成；查询与写接口留给 2.5-P-2。

产品边界变更：2.5 原本明确“不实现置顶”，经老大确认后纳入范围。侧栏 `ai-workspace-sidebar.tsx` 已有的 `pinnedThreads` / `onThreadPinToggle` 交互因此获得真实后端支撑。

- [x] **两组互斥列表**：固定会话与最近会话是两个互斥列表——固定列表数量有上限、一次性返回、按 `pinnedAt` 倒序；最近列表保持游标分页并排除已固定会话。这样分页列表始终只有 `updatedAt` 一个排序键，2.5-A-1 冻结的游标规则原样成立，不需要把固定状态编进游标；
- [x] **固定与归档互斥**：归档会话时同时清除 `pinnedAt`，因此固定列表永远不含已归档会话，固定接口不需要再叠加归档筛选；
- [x] `AiThread.pinnedAt` 可空列 + `@@index([ownerUserId, pinnedAt])`，migration `20260825153337_add_ai_thread_pinned_at` 为纯增量（可空列 + 新索引），无需回填、不影响既有数据，已应用到开发库与测试库；
- [x] 契约新增 `AI_THREAD_PINNED_MAX`（20）、`AiPinnedThreadList`、`SetAiThreadPinnedRequest`，`AiThreadListItem` 与 `AiThread` 增加 `pinnedAt`；
- [x] 新增错误码 `AI.THREAD_PINNED_LIMIT_EXCEEDED`（超出固定上限）与 `AI.THREAD_CURSOR_INVALID`（游标非法或伪造）；
- [x] 上限在**写入侧**强制：固定数不可能超过上限，因此固定列表的读取永远不会被截断，也就不需要游标或 `hasMore`。

#### 2.5-P-2：置顶列表查询与写接口

状态：✅ 已完成。

- [x] `AiThreadPinService.listPinnedThreads`：按 `pinnedAt` 倒序返回全部固定会话与当前上限，无分页；
- [x] `setThreadPinned`：**上限、幂等、归档互斥**三条规则集中在一个事务内实现；
- [x] **并发上限用按所有者的事务级 advisory lock 强制**。“先计数再写入”在默认 READ COMMITTED 下不安全：两个并发请求可以同时读到 19 再各写一条。选 `pg_advisory_xact_lock` 而不是锁 `User` 行，是为了不与登录、资料更新等无关写入互相阻塞；锁随事务自动释放。**该并发用例已验证有效性**：临时移除锁后测试立即失败（23 个并发请求只有 2 个被拒绝，实际固定 21 条突破上限），恢复后通过；
- [x] **置顶不改变 `updatedAt`**：`updatedAt` 是“最近”列表的排序键，若被置顶操作顺带刷新，取消置顶后这条陈旧会话会凭空跳到列表最前。写入时显式回写原值，并由真实 PostgreSQL 用例断言（同时验证了 Prisma 的显式赋值优先于 `@updatedAt`）；
- [x] 归档会话不可固定，返回新增的 `AI.THREAD_ARCHIVED`；非所有者统一返回 `AI.THREAD_NOT_FOUND`；
- [x] 抽出 `ai-thread-projection.ts` 统一列表、详情与固定列表的字段投影和转换，避免三个接口各自维护字段清单而出现差异；
- [x] `GET /ai/threads/pinned` 与 `PUT /ai/threads/:threadId/pinned`。**`pinned` 路由必须声明在 `:threadId` 之前**，否则会被当作 Thread 标识匹配。

验证：真实 PostgreSQL 新增 3 条（幂等且不改 `updatedAt`、并发不突破上限、归档与越权拒绝），AI 模块 15 个 Suite、155 条通过；三个包类型检查与 ESLint 通过。

留给 2.5-B：归档接口实现时需要同步清除 `pinnedAt`，保持与本轮的互斥规则一致。

完成门禁：

- 新建、分页、更新、归档和恢复接口通过权限测试；
- Thread 不因一个来源失权而整体报废，受影响的历史内容也不能泄漏；
- 中性占位不包含标题、摘要或其他可推断敏感信息。

### 7.6 步骤 2.6：现有工作区真实数据接入与阶段验收

状态：🟡 部分完成。第一版 UI 与 2.5 API 均已存在，但两者尚未接通。2.6 只做产品接入与验收，不新增业务工具、引用生成、检索能力或新的状态模型。

本步骤不承担 UI 重做。现有 AI 工作台的页面风格、信息架构、响应式布局、消息表现、工具状态、引用展示、动画和主要交互入口均视为已确认基线。接入真实数据时应优先适配现有组件职责；如果接口暂时不能提供某个字段，显示真实空状态、隐藏对应数据位或保留不误导用户的入口，不能使用静态模拟值伪装，也不能用旧版业务组件直接替换现有区域。

### 2.6 的客户端分层与协议约束

- 页面与路由只放在 `apps/web/src/app/(ai)/ai`；AI 业务状态、请求与事件处理全部放在 `apps/web/src/features/ai`。既有 `app/api/ai/**` 继续只做 BFF 转发、认证 Cookie 代理和 Runtime 调度，不把业务状态或权限规则写回 Route Handler。
- 本轮以 `features/ai/services/ai-thread-client.service.ts` 承担 REST 请求，以 `features/ai/hooks` 中单一的 Thread 工作区 Hook 承担路由、列表、消息、Run 与 SSE 状态；数据转换放 `features/ai/utils`，跨组件复用的领域类型放 `features/ai/types`。页面入口只组合布局与业务组件，不能继续把静态数据、请求和交互状态堆在 `ai-workspace-static.tsx`。
- 当前 `GET /api/ai/threads/:threadId/stream` 输出的是按 `AiEvent.sequence` 补拉的**标准 SSE 领域事件**，不是 Vercel AI SDK 的 UI Message Stream Protocol。根据 [AI SDK Transport 文档](https://ai-sdk.dev/docs/ai-sdk-ui/transport)，`useChat` 的默认 Transport 会 POST 到 `/api/chat`；根据 [AI SDK Stream Protocol 文档](https://ai-sdk.dev/docs/ai-sdk-ui/stream-protocol)，自定义后端要被 `useChat` 直接消费必须实现 UI Message Stream Protocol。因此 2.6 不复用当前 `useChat()` 默认调用，也不伪造协议；使用受控的浏览器 SSE 订阅 + 领域事件 reducer，再把持久化消息/事件适配给现有消息、工具卡和状态组件。若后续确有复用 `useChat` 的必要，必须先独立评审一个无状态的 UI Message Stream 适配层。
- `AiCitation` 尚未建立，引用卡在 2.6 只显示真实空状态；审批卡属于第 4 阶段，保持不可执行展示，不新增占位审批接口。当前 `/api/chat` 的 Mock 工具与联网检索开关不属于真实持久化闭环，接入时不得把它们伪装成真实业务工具。

### 2.6-A：工作区领域状态与路由基线

状态：🧪 待验证。代码实现已完成；定向 ESLint 通过，但尚缺真实账户手动验收与“切换期间旧请求不得覆盖新 Thread”的 Hook 定向测试，故不得标记为 ✅。

目标：移除静态会话数据，建立 Thread 路由与真实数据的单一状态入口。

- [x] 在 `features/ai/types` 定义仅供工作区使用的列表、消息视图模型与 Run 展示状态；复用 `@workspace/contracts/ai`，不复制服务端实体或新增跨端 UI 类型。
- [x] 在 `features/ai/hooks` 新建单一职责的 Thread 工作区 Hook：读取路由 `threadId`，并发加载固定列表、最近列表、详情与首屏消息；切换 Thread 时取消旧请求并重置旧流状态。
- [x] 用真实列表替换 `AiWorkspaceStatic` 中的静态 `pinnedThreads` / `recentThreads`，固定与最近保持服务端定义的互斥语义；URL 是当前会话的唯一来源，刷新与深链接可恢复。
- [x] 列表、详情和消息历史分别具备 loading / empty / error / success；游标“加载更多”不覆盖已加载消息，详情失败不把会话误显示为空。

非目标：不接发送、停止、重试或 SSE；不改造现有视觉层级。

实现记录：新增 `features/ai/types/ai-workspace.ts`、`features/ai/hooks/use-ai-thread-workspace.ts` 和消息分页合并工具；`/ai` 表示尚未创建 Thread 的新会话工作区，`/ai/:threadId` 作为既有 Thread 深链接。详情与消息请求使用 `AbortController` 和请求版本双重保护，旧请求不能写回新的路由工作区。旧静态数据文件已移除；现有聊天画布在 2.6-A 仅以只读模式保留视觉层级，禁止继续触发 `/api/chat` Mock，持久化消息画布和 SSE 仍严格留给 2.6-B。

验证：已通过本轮新增/修改 AI 工作区文件的定向 ESLint。Web `tsc --noEmit` 被既有 `.next/types` 中 5 个已删除 AI 路由的残留声明阻塞，新增文件未报告类型错误。仍待对真实账户手动验证新建空工作区、已有 Thread 深链接、切换 Thread、刷新恢复、游标加载更多；仍待补至少一组“切换期间旧请求返回不得覆盖新 Thread”的 Hook 定向测试。

### 2.6-B：持久化 Run 事件与消息画布适配

状态：🧪 待验证。浏览器侧代码与定向回归已完成，但真实账户和浏览器网络生命周期尚未验收。

目标：让已有消息画布消费数据库历史和标准 SSE 领域事件，而非 `/api/chat` 的临时 `useChat` 状态。

- [x] 在 `features/ai/utils` 定义纯 `AiEvent` reducer：按 `runId + sequence` 去重、顺序应用助手文本/工具状态/终态事件；未知事件保守忽略并记录开发期诊断，不把它当完成。
- [x] 在 `features/ai/hooks` 实现 SSE 生命周期：从已持久化 `lastSequence` 订阅，断线后以最后确认序号重连；切换 Thread、Run 终态、组件卸载时关闭旧订阅。
- [x] 将历史 `AiMessagePage` 与实时事件适配给现有 `AiChatSurface`、消息气泡和工具卡；失败、取消、取消中不能渲染为完成，来源失权只显示中性占位并隐藏工具信息。
- [x] 现有引用卡只接收真实 Citation 数据；本阶段无数据时显示真实空状态，不展示网页 Mock 来源。

非目标：不把 NestJS 领域 SSE 改写为 AI SDK UI Message Stream，不修改 Runtime、事件表或租约逻辑。

实现记录：新增 `features/ai/utils/ai-event-reducer.ts`、`features/ai/services/ai-thread-stream.service.ts` 和两组 Web Node 回归测试；扩展 Thread 工作区 Hook 维护 SSE 连接与最后确认序号；现有 `AiChatSurface` 改为消费历史/实时展示模型，移除默认 `useChat()` 消费和网页 Mock 来源，引用无真实数据时显示空状态。

验证：已通过 reducer/消息适配 6 条定向测试和本轮修改文件 ESLint；Web 类型检查只剩既有 `.next/types/validator.ts` 的 5 个已删除 AI 路由残留声明。真实账户下的同进程刷新、断线重连、两个 Thread 切换隔离和已有失权 Fixture 浏览器验证尚未完成，保留到 2.6-E 阶段验收。

### 2.6-C：发送、排队、调整方向与 Run 操作

状态：🧪 待验证。Web 代码实现与定向构建/回归已完成，真实账户下的浏览器交互、断线生命周期和停止/完成竞争仍留给 2.6-E 阶段验收。

目标：把输入框从 Mock Chat 改为 Thread 命令入口，并忠实表达 2.2/2.3 已冻结的运行语义。

- [x] 新会话首发调用 `POST /api/ai/threads`，已有会话调用 `POST /api/ai/threads/:threadId/messages`；每次用户动作生成稳定幂等键，成功后以服务端返回的投递状态、队列序号与 Run 标识更新本地状态。
- [x] 普通发送在活跃 Run 期间显示真实“排队中”和位置；“调整方向”显式使用 `STEER` 提交模式，显示“正在停止当前回答，随后按新方向继续”，不把新指令即时注入当前模型流。队列卡片独立于 `PromptInput` 的裁剪容器并位于正常文档流，避免输入框遮挡；编辑入口通过提交替代方向实现，删除入口在后端尚未提供消息删除契约前明确显示为未开放。
- [x] 接入停止和重试：停止只请求 Run 取消并等待 SSE/历史终态确认；重试只针对 `FAILED/CANCELLED` Run，成功后刷新权威 Thread 状态并订阅新 Run，不修改旧 Run 展示。
- [x] 区分提交失败、取消中、取消成功、被替代和服务端拒绝；命令失败保留输入内容，刷新或终态同步以持久化历史为准，不依赖内存标记补造服务端状态。

非目标：不新增消息编辑、分支会话、跨 Thread 调度或第 10 阶段端到端 Abort 保证。

实现记录：新增浏览器侧 AI 命令请求与响应契约、Thread REST 命令服务和 Hook 命令状态；新会话首发与既有 Thread 消息提交分别调用真实 BFF，普通消息在活跃 Run 期间显示服务端确认的队列序号，`STEER` 只请求有序取消并在终态后续跑。停止和重试接入现有 Run 接口，完成/取消终态后重新读取详情与消息，避免用本地状态替代持久化结果；失败消息增加重试入口。队列卡片在 `PromptInput` 外部正常布局，增加遮罩回归转换测试；联网检索保留视觉入口但明确标记暂未接入，未写入真实请求体。

验证：已通过 Web AI 消息/事件 8 条定向 Node 回归、修改文件定向 ESLint、contracts 类型检查和 Web production build；构建后 Web `tsc --noEmit` 通过。真实账户下关闭页面重开、浏览器断线重连、停止与完成竞争及桌面/移动端人工验收仍留给 2.6-E。

### 2.6-D：会话元数据操作

状态：🧪 待验证。侧栏代码实现与定向检查已完成，真实账户下的置顶上限、活跃 Run 归档拒绝、归档恢复和移动端抽屉交互仍留给 2.6-E 阶段验收。

目标：完成 2.5 已交付会话操作的 UI 入口，不改动聊天画布的运行状态机。

- [x] 接入置顶/取消置顶、重命名、归档/恢复；归档有活跃 Run、置顶超上限与非所有者等服务端错误按稳定错误码给出操作反馈，不做乐观伪成功。

非目标：不修改 SSE reducer、Run 调度或视觉布局；不新增批量操作、会话搜索和物理删除。

实现记录：新增独立 Thread 元数据 Hook 管理归档列表、元数据操作状态和稳定错误码文案；侧栏与移动端抽屉共用置顶/取消置顶、重命名 Dialog、归档/恢复入口。元数据接口成功后刷新服务端分组并同步当前 Thread，失败保留原列表和操作上下文；归档列表按需加载，避免改变首屏列表请求。2.6-C 的发送/停止/重试命令也抽为独立 Hook，工作区主 Hook 只保留路由、列表、消息和 SSE 状态协调。

验证：已通过 2.6-D 相关 Web 文件定向 ESLint、Web `tsc --noEmit`、contracts 类型检查，以及 Server AI 定向回归（15 Suite / 166 Tests）。真实账户下置顶上限、归档活跃 Run 被拒绝、归档后发送被拒绝、恢复后继续操作，以及非所有者不泄露存在性的错误反馈仍留给 2.6-E。

### 2.6-E：视觉、浏览器回归与阶段验收

状态：🧪 待验证。代码、自动化测试和不改变真实数据的浏览器验收已完成；需要人工确认后才能执行会创建消息、改变 Run 或修改会话元数据的浏览器写操作。

目标：在真实数据下确认已确认的工作台体验没有因接入而退化，并关闭第二阶段门禁。

- [x] 保留桌面侧栏、移动端抽屉、聊天画布、工具卡、动画和状态反馈；新会话入口已恢复为真实 Thread 命令入口，未接入的联网检索保持明确禁用状态，引用和审批未伪装为真实能力。
- [x] 对桌面与移动端检查 `min-h-0`、单一主滚动容器、固定输入区、长消息与工具卡溢出；桌面已测量聊天区与输入区无重叠，移动端已检查新会话输入和抽屉布局。
- [ ] 补最小浏览器端到端回归：已完成深链接恢复、只读 Thread 切换、归档列表和操作菜单；排队/调整方向、停止/重试、归档/恢复的真实写操作及来源失权占位仍待人工授权后的浏览器验收。API/事务安全继续复用 2.5 集成测试，不在前端复制 NestJS 权限逻辑。
- [x] 已在 Node 22.21.1 环境重跑 AI 定向测试、contracts/Web 类型检查、Web 全量 ESLint 和 production build，并同步更新本计划；Obsidian 实施/修复记录因当前工作区无指定外部目录写权限未更新。

非目标：不实现真实 `AiCitation`、审批执行、向量/关键词检索、跨进程续跑或新页面设计。

完成门禁：

- 关闭浏览器再打开，可以恢复已有 Thread、消息和最终 Run 状态；
- 失败和取消不会被显示为完成；
- 进行中 Run 在同一服务进程存活时可以补拉事件；
- 2.6-A～2.6-E 的每个增量都通过各自列出的定向验证；浏览器 E2E 只验证 UI 与 BFF 链路，不重复实现 NestJS 的权限判断；
- 浏览器不得再通过 `useChat()` 默认 `/api/chat` Transport 驱动真实持久化会话；真实会话只消费已定义的 REST + 标准 SSE 契约；
- `AiCitation`、审批和网页 Mock 工具在没有真实后端数据前不展示为真实能力；
- 数据接入前后主要业务区域、交互入口、视觉层级和动画表现保持一致；
- 桌面和移动端不存在双重滚动、内容截断或工作区撑破；
- 在 Node 22 环境完成第二阶段全部直接相关自动化测试、类型检查和人工验收通过。

---

## 8. 数据模型与约束草案

下表是实施分配，不代表字段已经冻结。字段级设计必须在对应增量开始前再次评审。

| 模型 | 引入步骤 | 核心职责 | 关键约束 |
| --- | --- | --- | --- |
| `AiThread` | 2.2-A/2.2-C | 用户会话、归档状态、当前活跃 Run、队列序号来源 | 所有者必填；不含业务目标字段；活跃 Run 与 Thread 一致；队列序号原子分配 |
| `AiMessage` | 2.2-A/2.2-C | 用户/助手展示消息及用户输入的投递状态 | Thread 内稳定顺序；客户端请求幂等；用户排队项有 Thread 内唯一队列序号；正文不能承担工具原文存储 |
| `AiRun` | 2.2-A | 一次执行、状态、模型、用量、取消、重试、租约 | 单 Thread 单非终态 Run；终态不可回退；重试关联旧 Run |
| `AiEvent` | 2.2-A | 流式恢复和审计事件 | `(runId, sequence)` 唯一；只追加，不原地改写历史事件 |
| `AiStep` | 2.4 | 一次模型步骤和停止原因 | `(runId, sequence)` 唯一；保存受控 Token 摘要；已随 `20260825140000` migration 落地 |
| `AiToolCall` | 2.4 | 工具输入摘要、状态、耗时和错误 | 不保存密钥；`(runId, providerToolCallId)` 唯一；超长输出只保留截断标记；状态与结果字段由 CHECK 约束保证一致；已随 `20260825140000` migration 落地 |
| `AiSourceDependency` | 2.4/2.5 | Run 实际读取或引用的业务来源 | `(runId, sourceType, sourceId, usage)` 唯一，可按 `(sourceType, sourceId)` 反查受影响 Run；区分 READ 与 CITED；2.4 只登记 READ，已随 `20260825140000` migration 落地 |
| `AiCitation` | 第 5 阶段基础引用；第 9 阶段 Claim 级引用与 Evidence Receipt | 生成内容与稳定来源的映射 | 第 5 阶段绑定助手消息/草稿主张、证据来源和安全定位；只能指向本 Run 的允许来源集合，失权时不泄露标题、片段或定位信息 |
| `AiApproval` | 第 4 阶段 | 写操作或高风险工具审批 | 第二阶段不提前启用空审批流程 |

### 8.1 单活跃 Run 的数据库约束

`AiThread.activeRunId` 可以作为事务比较更新和快速读取指针，但只靠应用判断不够。2.2-A 需要评审以下方案：

1. `activeRunId` 可空唯一外键，并由所有状态事务同步维护；
2. PostgreSQL 对 `AiRun(threadId)` 增加“非终态状态”的部分唯一索引，作为最终并发硬门禁；
3. 同时使用两者时，必须在同一事务更新并增加一致性对账，避免形成两个权威来源。

当前推荐同时保留快速指针和部分唯一索引，但在 migration 前必须确认 Prisma 迁移、测试数据库和回滚方案能够稳定支持自定义 SQL。

### 8.2 事件序号

- 不使用 `SELECT MAX(sequence) + 1`；
- 推荐在 `AiRun` 保存下一事件序号，通过原子更新分配单个序号或序号区间；
- `AiEvent` 使用 `(runId, sequence)` 唯一约束兜底；
- 事件批量追加和 Run 状态更新是否共用事务，由 2.2-B 的性能与一致性测试决定；
- 前端只使用服务端事件序号，不使用本地数组下标恢复流。

### 8.3 幂等键

至少需要区分：

- 创建 Thread：当前用户 + 创建请求键；
- Thread 内发送消息：Thread + 当前用户 + 消息请求键；
- 重试 Run：旧 Run + 当前用户 + 重试请求键。

同一键但请求正文不同应返回 `AI_IDEMPOTENCY_CONFLICT`，不能静默重放旧结果。幂等键保留时间、字段落点和是否单独建立请求表仍是 2.2-B 的待决策项。

### 8.4 输入队列的候选字段

2.2-C 开始前再冻结具体命名。当前候选是为用户消息增加 `dispatchState`（如 `QUEUED`、`DISPATCHED`、`SUPERSEDED`）、`queueSequence` 和 `submissionMode`（如 `NORMAL`、`STEER`），并在 Thread 上保存下一可分配队列序号。这里的排队状态不替代 `AiRun.status`：前者描述一条用户输入是否待分发，后者描述已经创建的实际执行。

若保留“最新定向请求替代旧定向请求”策略，旧请求必须转为可见、可审计的 `SUPERSEDED` 或等价状态，而不是删除消息；最终枚举名称、索引和保留策略由 2.2-C migration 评审确定。

---

## 9. 最小浏览器 API 草案

以下是 Next.js BFF 对浏览器提供的目标契约。BFF 不拥有最终权限和状态规则。

- `POST /api/ai/threads`：接收首条非空消息和幂等键，原子创建 Thread、Message 和 Run；
- `GET /api/ai/threads`：按游标和归档状态过滤当前用户 Thread；
- `GET /api/ai/threads/:threadId`：获取 Thread 元数据和最近 Run 摘要；
- `GET /api/ai/threads/:threadId/messages`：游标分页获取消息及允许展示的工具和引用数据；
- `POST /api/ai/threads/:threadId/messages`：接收用户消息、幂等键和提交模式（普通发送或“调整方向”）；原子入队并返回投递状态、队列位置以及已创建的 Run（如有）；
- `GET /api/ai/threads/:threadId/stream?runId=...&afterSequence=...`：订阅或补拉已有 Run，绝不启动新执行器；
- `POST /api/ai/runs/:runId/stop`：请求停止当前 Run；
- `POST /api/ai/runs/:runId/retry`：从失败或取消 Run 创建关联的新 Run；
- `PATCH /api/ai/threads/:threadId`：只允许重命名、归档和恢复归档。

NestJS 还需要受服务身份保护的内部执行契约，用于领取、续租、事件追加、工具来源登记和终态写入。浏览器不得直接调用这些内部接口。

---

## 10. 测试与验收矩阵

| 测试域 | 必须覆盖的场景 | 对应步骤 |
| --- | --- | --- |
| 契约 | Thread 不含业务目标字段、创建请求只含消息和幂等键、稳定错误码 | 2.1 |
| 状态机 | 全部合法/非法转换、终态不可回退 | 2.1 |
| Migration | 应用、回滚、CHECK、索引和外键 | 2.2-A |
| 幂等 | 相同键重放、同键不同正文冲突、跨用户不串请求 | 2.2-B |
| 并发 | 同 Thread 不同键并发，只产生一个非终态 Run | 2.2-B |
| 输入队列 | 活跃 Run 下普通输入持久化排队、FIFO、幂等重放、并发入队稳定、刷新后可恢复 | 2.2-C/2.5/2.6 |
| 事件 | 序号唯一、并发追加、断线补拉不重不漏 | 2.2-B |
| 租约 | 单执行器领取、续租、过期和迟到写入拒绝 | 2.3 |
| 取消与定向 | 取消/完成竞争、停止中 UI、唯一终态；定向请求先取消旧 Run、迟到写入拒绝、仅优先分发一次 | 2.3/2.6 |
| 故障 | Web Runtime 重启、租约过期、对账释放门禁 | 2.3 |
| 实体发现 | 自然语言名称/别名唯一命中后读取上下文；重名、无结果和无权候选只追问或返回允许展示的候选；不得猜测 ID | 2.4 |
| 工具权限 | 用户消息指定目标后查询授权 Decision；项目和私有区域越权为零 | 2.4 |
| 权限变化 | 单来源失权、执行中撤权、来源删除 | 2.4/2.5 |
| 历史 | 列表、详情、消息、工具、引用和事件无旁路泄漏 | 2.5 |
| UI 接入 | 在现有第一版 UI 内完成多 Thread、路由恢复、四态、真实工具/引用状态以及桌面和移动端滚动；对照接入前后检查视觉和功能完整性 | 2.6 |
| 上下文预算 | 长历史只注入受限最近消息，完整历史仍可分页查看 | 2.4/2.6 |

权限泄漏继续执行零容忍门禁。普通质量指标可以逐步改善，但任何越权来源都不能被平均分掩盖。

---

## 11. 执行纪律

1. 每轮只完成一个目标步骤内的一个最小增量。
2. 动手前说明本轮目标、不做什么、主要文件和验证方式。
3. 影响超过当前增量时停止并重新确认，不顺势扩大。
4. 当前增量通过门禁后停止，等待老大检查或明确要求进入下一步。
5. 涉及 Prisma 时只生成新 migration，不修改历史 migration 或 generated client。
6. 接入真实数据时保留当前新版 AI 工作台的信息架构、视觉和交互入口。
7. 完成代码和验证后同步更新 Obsidian 的 `AI模块/AI第二阶段实施记录.md`；无法写入时必须明确说明。
8. 每次修改本文都同步更新“当前实现审计”“待决策项”和“变更记录”。

---

## 12. 当前待决策项

以下问题未确认前，不应开始依赖它的 migration 或接口实现。

| 编号 | 决策问题 | 当前建议 | 最晚确认时间 | 状态 |
| --- | --- | --- | --- | --- |
| D2-01 | Thread 是否保存项目、决策或会议上下文 | 不保存；业务目标全部来自用户消息，Agent 负责发现和消歧 | 2.1 | 已确认 |
| D2-02 | 业务目标如何落库 | 不在 Thread 落库；只登记每个 Run 实际读取和引用的来源依赖 | 2.1/2.4 | Thread 契约与 `AiSourceDependency` 已落地 |
| D2-03 | 单个来源失权是否锁整条 Thread | 不锁整条，只锁或隐藏受影响的 Run/消息/工具结果/引用 | 2.1/2.5 | 来源依赖已落地；历史权限投影留给 2.5-C |
| D2-04 | 单活跃 Run 的硬约束 | `activeRunId` + PostgreSQL 非终态部分唯一索引 | 2.2-A | 已确认并落地 |
| D2-05 | 事件序号如何分配 | `AiRun.nextEventSequence` + 原子 `UPDATE ... RETURNING`，不使用 `MAX + 1` | 2.2-A | 已确认并落地 |
| D2-06 | 幂等键保存方式和有效期 | 幂等键和请求指纹分别随 Thread 创建、用户消息和重试 Run 保存；与资源同生命周期保留；同键不同正文报冲突 | 2.2-B | 已确认并落地 |
| D2-07 | 来源权限恢复后是否自动解锁历史 | 默认不自动，先要求全部依赖重新可见并显式重评 | 2.5 | 待设计 |
| D2-08 | 第二阶段最小真实工具链 | `findDecisionCandidates` + `getDecisionContext`；用户输入名称/别名/可见标识符，唯一命中后读取，重名则消歧 | 2.4 | 已确认、落地并验证 |
| D2-09 | 活跃 Run 期间再次发送的语义 | 普通发送持久化排队；“调整方向”持久化后请求取消当前 Run，并在其终态后优先续跑；不直接注入当前模型流 | 2.2-C/2.3 | 已确认并落地 |
| D2-10 | 连续多次“调整方向”的处理 | 最新定向请求替代全部尚未领取的旧用户输入；旧普通与旧定向输入均保留为 `SUPERSEDED` 审计状态，不再执行 | 2.2-C | 已确认并落地 |
| D2-11 | 初始执行租约时长 | 固定为 30 秒；每次合法续租按同一时长滚动延长。最大运行时长与生产环境配置化不属于第二阶段 | 2.3 | 已确认并落地 |
| D2-12 | Next.js Runtime 调用 NestJS 内部执行接口如何鉴权 | 共享密钥环境变量 `AI_RUNTIME_SERVICE_TOKEN`：只有 Next.js 服务端持有，NestJS 用定长比较校验请求头，未配置密钥时全部拒绝；浏览器与 BFF 转发路径都不携带该值 | 2.4 | 老大已确认并落地 |
| D2-13 | 工具串联规则写在哪里 | 不写进单个工具，也不在编排层写死工具名：需要“目标先被唯一发现”的工具在描述中声明 `discoveryRequirement`，编排层统一强制，规则判定只依赖已持久化的工具调用记录 | 2.4 | 已确认并落地 |

---

## 13. 下一轮建议

**2.5 已完成并在独立测试库复验通过；2.6-A～E 代码与自动化/只读浏览器验收已完成，写操作门禁仍待授权；2.7-A～D 已完成，2.7-E～F 代码完成待真实浏览器验收，灰度/正式回滚后置。** 当前先完成 2.7-E/F 浏览器验收，通过后再进入 **2.7-G：队列、调整方向、停止与重试闭环**。

2.6 是 UI 接入，性质与前面几轮不同，建议仍按最小步骤推进，且优先处理下列已知约束：

1. **不得视觉回退**：现有 AI 工作台的信息架构、布局、动画与交互入口是已确认基线，接真实数据时不能用旧版组件替换，也不能静默删除已有区域；
2. **置顶入口现在有后端支撑了**：侧栏 `pinnedThreads` / `onThreadPinToggle` 可以接 2.5-P 的真实接口；
3. **失权占位的文案红线**：见 2.5-C-2，不得包含来源标题、摘要、名称或数量；
4. **引用位保持真实空状态**：`AiCitation` 表不存在，不得用静态值伪装；
5. **四态齐全**：列表与聊天分别处理 loading / empty / error / success，并检查 `min-h-0`、滚动容器与响应式溢出。

> **顺序约束**：2.5-C 必须排在 2.5-D 之前。历史接口目前返回未经权限投影的消息正文，在投影落地前不应通过 BFF 暴露给浏览器。

已知遗留：

- **对账查询索引**：过期租约扫描与孤儿排队 Run 扫描都没有对应索引（`AiRun` 现有索引为 `[threadId, status]`），随 `AiRun` 增长会退化为顺序扫描。补 `[status, createdAt]` 类索引需要新的 migration，建议在 2.5 或性能专项中一并评估；
- **既有 meetings 问题**：`meetings.service.spec.ts` 的 2 处 fixture 类型错误与 3 条会议时间断言失败为既有问题，与 AI 模块无关，需单独修。
- **测试准备链路**：`test:db:create` 目前只创建数据库；运行完整 AI 集成测试前还必须对测试库执行 migration，并重新生成 Prisma Client。该步骤应在开始 2.6 前收敛为一个可重复命令或 CI 前置步骤，属于测试可靠性改进，不属于 2.6 的产品功能。
- **运行时版本**：本轮已在 Node 22.21.1、pnpm 10.24.0 下完成 2.6-E 的 Web/Server 定向验证；后续如切换本机运行时仍需按目标版本复验。

本轮 2.6-E 的代码、自动化和只读浏览器验收到此完成；真实账号写操作门禁待老大确认后再执行。

---

## 14. 变更记录

| 日期 | 版本 | 变更 |
| --- | --- | --- |
| 2026-08-27 | v2.28 | 完成 2.7-F 代码实现：新 Thread 创建支持统一 POST SSE，新增稳定布局 `AiPostStreamProvider` 托管跨 `/ai` 与 `/ai/:threadId` 的读取器和事件回放，`submission` 后安全切换 URL；新增新会话 Transport 回归，37 条 Web AI 定向测试、TypeScript、修改文件 ESLint 和 diff 检查通过。真实新会话浏览器验收待执行，未修改 UI、Prisma 或灰度配置。 |
| 2026-08-27 | v2.27 | 完成 2.7-E 代码实现：已有 Thread 发送切换为 POST SSE Transport，提交后复用同一 reducer 立即消费 live delta，持久化确认按 `liveDeltaId` 去重，流异常/handoff 自动复用 GET SSE；新增 Transport/Reducer 回归，与既有 Runtime、协议和工作区测试通过；Web TypeScript、修改文件 ESLint 和 diff 检查通过。真实已有 Thread 浏览器验收待执行，未修改新会话、UI、Prisma 或灰度部署配置。 |
| 2026-08-27 | v2.26 | 根据老大确认，当前个人项目不引入 `AI_DIRECT_STREAM_ENABLED`、灰度发布或正式自动回滚流程；保留 `Accept` 分流、JSON 兼容路径和 GET SSE 恢复作为低成本方案，未来有真实线上部署需求时再单独评估。 |
| 2026-08-27 | v2.25 | 完成 2.7-D：已有 Thread 的 POST 在显式 `Accept: text/event-stream` 下先返回幂等 `submission`，再直出当前 Run 的 live delta、持久化事件、终态状态和 handoff；保留原 JSON 路径，客户端断开不取消 Runtime，后继排队 Run 交回后台链。新增 4 条 POST 响应桥回归，与协议/live sink 合计 10 条通过；Web TypeScript、修改文件 ESLint 和 diff 检查通过；未修改 NestJS、Prisma、GET SSE、新会话路由或 UI。 |
| 2026-08-27 | v2.24 | 完成 UI-first 方案下的 2.7-C：模型 delta 立即直出，按 Run 串行、有界异步持久化并在终态前冲刷；持久化事件携带 live delta 关联信息，队列溢出/持久化失败阻止 Run 正常完成。Web Runtime 16/16、协议 3/3、Server AI 15 Suite / 168 Tests（真实 PostgreSQL 集成 32 条）、contracts/Web 类型检查和定向 ESLint/Prettier 通过；未接入浏览器 POST、未修改 Prisma/GET SSE，未新增 Markdown，未删除长期回归测试。 |
| 2026-08-27 | v2.23 | 按老大确认将 2.7 改为 UI-first：模型 delta 通过 `live-delta` 立即直出，`AiEvent` 异步持久化作为恢复确认；新增 `liveDeltaId/liveSequence` 关联与去重规则，扩展 live sink 消息形状。2.7-C 原基础保留但需补异步持久化队列，2.7-D 暂缓；未新增 Markdown、未删除长期回归测试。 |
| 2026-08-27 | v2.22 | 完成 2.7-C：新增进程内有界 Runtime live sink，已提交文本/工具/终态事件按序发布，支持可选 sink、慢消费者关闭、写入失败隔离和无 sink 兼容路径；Runtime 文本批次初始调整为 `40` 字符/`100ms`。Web Runtime 13/13、Web 类型检查、定向 ESLint/Prettier 通过；未接入浏览器 POST 流、未修改 Route Handler、GET SSE、Prisma 或数据库；未创建一次性测试文件。 |
| 2026-08-27 | v2.21 | 完成 2.7-B：NestJS 文本增量、工具开始/结束和 Run 终态内部接口返回事务提交后的权威事件回执；公共停止接口保留最小响应并隔离内部审计数据。Server AI 15 Suite / 167 Tests、真实 PostgreSQL 集成 31 Tests、工具编排 11 Tests、contracts/Web 类型检查和修改文件 ESLint/Prettier 通过。未修改 Prisma、Route Handler、GET SSE 或浏览器推流，未创建一次性测试文件。 |
| 2026-08-27 | v2.20 | 完成 2.7-A：新增 POST 直出流五类帧共享契约、提交/状态/handoff/流错误结构，新增支持任意 chunk 边界的纯 SSE 编解码器与 Node 回归；冻结响应头前后错误语义、帧顺序/重复规则和改造前基线记录。未修改 Route Handler、Runtime、NestJS、Prisma 或现有 GET SSE，2.7-B～I 保持未执行。 |
| 2026-08-26 | v2.19 | 完成 2.6-E 的代码与自动化/只读浏览器验收：发现并修复 `/ai` 新会话只显示空文案、无法进入首条消息发送的问题，现已复用真实 Thread 命令入口；补充移动端抽屉的无障碍描述。Node 22.21.1 下 Web 全量 ESLint、Web AI 8 条回归、contracts 类型检查、Server AI 15 Suite / 166 Tests 和 Web production build 通过。已在登录 Chrome 中只读验证已有 Thread 深链接恢复、归档列表读取、归档 Thread 切换、桌面聊天区单一滚动容器、移动端抽屉与重命名 Dialog；未执行会改变真实会话/Run 的写操作，相关浏览器门禁仍待人工确认。 |
| 2026-08-26 | v2.18 | 完成 2.6-D Web 代码实现：接入 Thread 置顶/取消置顶、重命名、归档/恢复真实接口；侧栏与移动端抽屉共用元数据操作，新增按需归档列表和 shadcn 重命名 Dialog。所有变更仅在服务端成功后刷新分组，置顶上限、活跃 Run 归档、已归档操作和会话不存在等稳定错误码转换为中文反馈；不改动聊天画布、SSE 或 Run 状态机。定向 ESLint、Web `tsc --noEmit`、contracts 类型检查、Server AI 15 Suite / 166 Tests 和 Web production build 通过；真实浏览器验收留给 2.6-E。 |
| 2026-08-26 | v2.17 | 完成 2.6-C Web 代码实现：新增 AI Thread 命令请求/响应契约与浏览器 REST 服务；新会话首发、既有 Thread 发送、普通排队、`STEER` 调整方向、停止和失败/取消重试均接入真实 BFF，幂等键由每次用户动作稳定生成并以引用门禁防止重复点击。历史 `QUEUED` 用户输入移到输入框上方的独立队列卡片，卡片与 `PromptInput` 保持正常文档流，修复遮挡；队列动作菜单保留编辑替代与暂未开放删除状态。新增队列消息适配回归；定向 Node 测试 8/8、ESLint、contracts 类型检查、Web production build 与构建后 Web 类型检查通过；真实浏览器验收留给 2.6-E。 |
| 2026-08-26 | v2.16 | 完成 2.6-B 代码实现：新增纯 `AiEvent` reducer，按 Run 序号去重并缓存乱序事件，未知事件保守忽略；新增 EventSource 领域 SSE 订阅服务，工作区按最后确认序号重连并在 Thread 切换、Run 终态和卸载时关闭；历史消息与实时助手正文/工具状态接入既有 `AiChatSurface`，失败/取消不显示完成，来源失权显示中性占位并隐藏工具，引用无真实数据时显示空状态；新增 6 条 Web Node 回归测试。定向 ESLint 通过，Web 类型检查仍被既有 `.next/types/validator.ts` 的 5 个已删除 AI 路由残留声明阻塞；真实浏览器验收留给 2.6-E。 |
| 2026-08-26 | v2.15 | 修复阻塞 2.6-A 验收的 NestJS 启动故障。根因是 Server 编译为 CommonJS 后以 `require()` 直接加载 `@workspace/contracts/*` 的 TypeScript 源码，Node 22.21.1 在该 CJS/ESM 边界抛出 `ERR_INTERNAL_ASSERTION`。contracts 新增运行时构建配置，`exports` 按 `types`/`require`/`default` 分流：NestJS 读取 `dist` 的 CommonJS JS，Web 构建继续读取源码；Server `predev` 会先构建运行时 contracts。直接 `require('@workspace/contracts/ai')` 已通过，Nest 已完成全部模块初始化；验证进程仅因 3001 已被现有服务占用而退出，原模块加载错误未再出现。 |
| 2026-08-26 | v2.14 | 完成 2.6-A 代码实现并同步实施状态。静态 `AiWorkspaceStatic` 会话数据替换为真实 Thread 工作区：`/ai` 为新会话入口、`/ai/:threadId` 支持深链接恢复；新增工作区专用类型、路由 Hook 与消息分页合并工具，固定/最近列表、详情和首屏消息并发通过既有 BFF 读取。Thread 切换使用 `AbortController` 与请求版本拒绝旧响应覆盖新路由。为保持本增量边界，既有聊天画布仅保留只读视觉壳并禁用 `/api/chat` Mock；持久化消息渲染与 SSE 明确留给 2.6-B。定向 ESLint 通过；Web 类型检查受既有 `.next/types` 的 5 条失效路由声明阻塞；Hook 定向测试与真实账户人工验收尚未完成，因此 2.6-A 标记为 🧪 待验证。 |
| 2026-08-26 | v2.13 | 细化 2.6 为 A～E 五个可独立审查的增量，并冻结客户端分层与协议边界：页面/Route Handler 保持薄层，工作区状态进入 `features/ai/hooks`，REST 请求复用 `services`，事件归约进入 `utils`。核对 Vercel AI SDK 官方 Transport 与 Stream Protocol 后明确：当前持久化 Run 输出的是标准 SSE 领域事件，不是 UI Message Stream Protocol，因此不再让 `useChat()` 默认 `/api/chat` Transport 承担真实会话；改由受控 SSE Hook 订阅已持久化事件并适配现有 UI。同步明确 Citation、审批与网页 Mock 工具均不在 2.6 伪装为真实能力；补齐逐增量验证和 Node 22 阶段门禁。 |
| 2026-08-26 | v2.12 | 独立复核 2.5。先发现并修复测试基础设施缺口：`Sliye_test` 缺失、Prisma Client 未包含 `pinnedAt`；已创建/确认独立库、对其应用 24 条 migration，并执行 `prisma generate`，不涉及开发库数据。复验 `pnpm --filter @nextnest/server test -- --runInBand modules/ai` 为 **15 Suite / 166 Tests 通过**，contracts 与 Web 类型检查通过。范围结论：2.5 已交付计划内的历史、元数据、来源失权投影和 BFF；AI 工作台仍是静态页面，属于明确留给 2.6 的真实数据接入，不构成 2.5 漏项。同步修正 2.5 总清单中遗留的未勾选项，并补充测试准备链路风险；另记录本机 Node 18.20.3 与仓库 Node 22 声明不一致，目标运行时仍需复验。 |
| 2026-08-26 | v2.11 | 完成 2.5-D，**2.5 全部收尾**。补齐 Thread 列表/详情、消息历史、固定列表、固定/重命名/归档恢复的 Next.js BFF 路由，代理方法补上 `PUT`。BFF 刻意不做任何权限判断也不改写响应——转发层再写一套规则必然与服务端漂移。`/api/ai/threads/pinned` 依靠 Next.js 静态段优先于动态段的匹配顺序与 `[threadId]` 区分。新增旁路越权回归用例，用两个真实用户覆盖列表、直接 URL、消息（工具结果的唯一出口）与事件补拉四类入口，事件补拉额外验证“正确 runId 配错误 threadId”同样被拒，并断言所有者正常组合仍可用。清理已被真实路由取代的占位文件，并删除指向不存在接口的 `[threadId]/stop` 目录（停止是 Run 级操作）。AI 与 decisions 模块 19 个 Suite、214 条通过。 |
| 2026-08-26 | v2.10 | 完成 2.5-C-2：把失权判定投影到消息输出，**NestJS 侧 2.5 全部就绪**。契约新增 `contentVisibility`（`VISIBLE`/`SOURCE_REVOKED`），在类型层明确区分"被隐藏"与"没有回答"。失权时清空 `content` 并**一并清空 `run.toolCalls`**——工具名称与数量同样可反推出用户无权知道的信息；但保留 Run 状态与失败/取消原因，因为它们描述用户自己这次运行的结果，不承载来源内容。用户消息永不隐藏。整页一次性批量判定，`listMessages` 改收 `AuthorizationContext`。真实 PostgreSQL 用例断言整页序列化后不含助手正文、决策标题与工具名，并覆盖"撤权隐藏 → 恢复后实时可见"。记录占位红线：`AiSourceDependency.label` 不得用于渲染失权提示。AI 与 decisions 模块 19 个 Suite、213 条通过。 |
| 2026-08-26 | v2.9 | 完成 2.5-C-1：来源失权判定逻辑。**D2-07 确认为方案 A**（实时判定，权限恢复后自动重新可见），因此不引入任何持久化锁定状态。新增 `DecisionVisibilityService`（decisions 模块，复用 `buildDecisionWhere('decision:read')` 批量筛可见决策与决议，决议可见性等同所属决策）与 `AiSourceVisibilityService`（ai 模块，按 Run 批量判定，按来源类型各发一次查询避免 N+1，无来源依赖的 Run 视为可见）。未知来源类型与无法解析的 sourceId 一律 **fail closed** 判定为不可见，保证将来漏补判定时是过度隐藏而非越权泄漏。真实 PostgreSQL 用例覆盖“可见 → 撤权后不可见 → 恢复后自动重新可见 → 非法标识 fail closed”完整生命周期。AI 与 decisions 模块 19 个 Suite、212 条通过。 |
| 2026-08-26 | v2.8 | 完成 2.5-B：会话重命名、归档与恢复。新增 `AiThreadMetadataService`，接口 `PUT /ai/threads/:threadId/title` 与 `PUT /ai/threads/:threadId/archived`。关键决策：（1）确立并写进契约的 **`updatedAt` 不变式**——只有会话活动推进它，重命名/固定/归档/恢复一律保持原值，避免改标题让陈旧会话跳到列表最前；（2）**归档要求没有活跃 Run**（老大确认），返回既有 `AI.THREAD_RUN_ACTIVE`，判断与写入复用 `lockThread` 同一把行锁以防竞态，不让归档隐式取消正在生成的回答；（3）归档清除固定、恢复不自动重新固定。顺带闭合状态矛盾：原先仍可向已归档会话发消息从而产生“已归档+有活跃 Run”，现已在同一把锁内拒绝并返回 `AI.THREAD_ARCHIVED`。AI 模块 15 个 Suite、163 条通过。 |
| 2026-08-26 | v2.7 | 完成 2.5-A-3：Message 游标分页。新增 `ai-message-history.types.ts` 契约与 `AiMessageQueryService`，接口 `GET /ai/threads/:threadId/messages`。关键决策：（1）`items` 按时间**正序**返回、游标向更早推进，顺序在契约层固定，避免各客户端各自反转；（2）**不返回工具输入与输出**——工具卡本就只渲染名称与状态，而 `outputSummary` 承载真实业务事实，返回它会让历史接口在来源失权后变成泄漏面，因此从源头不制造需要 2.5-C 补救的问题，并有用例断言其不出现在响应中；（3）游标编解码抽为通用 `ai-cursor.ts` 并引入 `kind`/`scope` 判别，会话游标不能翻消息列表、消息游标不能跨会话，错误码泛化为 `AI.CURSOR_INVALID`。顺带修复集成测试清理钩子的删除顺序缺陷（删 Run 会把助手消息 `runId` 置空从而违反 CHECK 约束），并让清理在 `beforeAll` 也执行一次以自愈残留数据。记录模型张力：`SetNull` 外键与 CHECK 约束组合导致助手消息存在时 Run 无法删除，当前无删除路径故仅为潜伏问题。AI 模块 15 个 Suite、160 条通过。 |
| 2026-08-26 | v2.6 | 完成 2.5-P-2：置顶列表查询与置顶/取消写接口。新增 `AiThreadPinService`，把上限、幂等、归档互斥三条规则集中在单个事务内。两个关键设计：（1）并发上限用按所有者的 `pg_advisory_xact_lock` 强制——“先计数再写入”在 READ COMMITTED 下会被并发突破，选 advisory lock 而非锁 `User` 行以免阻塞无关写入；该用例已用“临时移除锁 → 测试失败（实际固定 21 条）→ 恢复 → 通过”验证有效性；（2）置顶不刷新 `updatedAt`，否则取消置顶会让陈旧会话跳到“最近”列表最前，写入时显式回写原值并由用例断言。新增错误码 `AI.THREAD_ARCHIVED`；抽出 `ai-thread-projection.ts` 统一三个接口的字段投影；新增 `GET /ai/threads/pinned`（须声明在 `:threadId` 之前）与 `PUT /ai/threads/:threadId/pinned`。同时修正计划账目：Thread 列表/详情与 `POST /messages` 两项早已完成但一直未勾选；并记录 `AiCitation` 表不存在、2.5 无法交付引用展示数据。AI 模块 15 个 Suite、155 条通过。 |
| 2026-08-25 | v2.5 | 完成 2.5-A-2：Thread 列表/详情只读实现。新增 `ai-thread-cursor.ts`（不透明游标编解码，结构校验而非签名——数据范围由所有者条件独立保证，故不引入 HMAC）与 `AiThreadQueryService`（keyset 分页、归档筛选、排除已固定、详情用既有类型守卫收窄活跃 Run），新增 `GET /ai/threads` 与 `GET /ai/threads/:threadId`。把"切换 filter 复用旧游标"从契约里的"行为未定义"改为**稳定报错** `AI.THREAD_CURSOR_INVALID` 并同步修正契约注释。修复测试环境问题：2.4-L 移除 Jest 的 `.env` 加载后，把测试库连接串写进 `.env` 不生效；新增 `test/jest-setup-ai-test-db.ts` **只注入** `AI_PERSISTENCE_TEST_DATABASE_URL` 一个变量，并在它与 `DATABASE_URL` 指向同一数据库时直接抛错终止（集成测试会无条件清表），无法比对时打印警告而不静默放行。AI 模块 15 个 Suite、152 条通过，其中真实 PostgreSQL 18 条（新增同 `updatedAt` 翻页稳定性与详情所有者边界）。 |
| 2026-08-25 | v2.4 | **产品边界变更**：老大确认把"会话置顶"纳入 2.5 范围（原计划明确排除）。完成 2.5-P 数据模型与契约：`AiThread.pinnedAt` 可空列 + `@@index([ownerUserId, pinnedAt])`，migration `20260825153337_add_ai_thread_pinned_at` 为纯增量；契约新增 `AI_THREAD_PINNED_MAX`(20)、`AiPinnedThreadList`、`SetAiThreadPinnedRequest`，并新增 `AI.THREAD_PINNED_LIMIT_EXCEEDED`、`AI.THREAD_CURSOR_INVALID` 两个错误码。关键设计：固定与最近是**两个互斥列表**（固定有上限一次性返回、最近游标分页且排除已固定），使 2.5-A-1 的单排序键游标规则原样成立；上限在写入侧强制，因此固定列表读取永不截断；归档会清除固定状态。同时建立隔离测试库：新增 `test:db:create` 脚本（复用 `DATABASE_URL` 连接信息只换库名，已存在则跳过），迁移已同步，`.env.example` 补 `AI_PERSISTENCE_TEST_DATABASE_URL` 说明。AI 模块 13 个 Suite、135 条测试全部通过（集成用例现已随普通测试命令运行）。 |
| 2026-08-25 | v2.3 | 开始 2.5：完成 2.5-A-1 契约冻结。新增 `ai-thread-history.types.ts`，定义 Thread 列表查询、列表项、分页结果、活跃 Run 摘要与详情响应。关键约定：列表按 `updatedAt` 倒序并使用**不透明游标**（内部承载排序时间 + Thread 标识，保证同时间戳下不重复不漏项，客户端不得自行构造，切换 filter 必须重置）；列表项不返回 `ownerUserId`（恒等于调用方）；`AiThreadActiveRun.status` 收窄为 `AiRunNonTerminalStatus`，从类型层面排除返回终态活跃 Run；详情不内联消息。三个包类型检查通过，无重名冲突。记录已知缺口：侧栏"置顶"交互在 2.5 范围外无后端支撑，需在 2.6 决定保留标记还是隐藏。 |
| 2026-08-25 | v2.2 | 新增 2.4-M：修复 2.4 代码评审发现的 5 个问题。（1）过期租约对账补孤儿 `QUEUED` Run 分支——这类 Run 没有租约、SQL 比较永不成立，调度回调丢失后会永久占住 `activeRunId` 堵死整条 Thread；（2）事件补拉的 `threadId` 改为必填 DTO，原裸参数缺失时会被 Prisma 静默丢弃，2.4-K 声称的三重校验对直接调用不成立；（3）截断约定抽到 `ai-tool-output-summary.ts`，重放识别到截断标记改判 `REPLAY_UNAVAILABLE`，不再把 `{truncated:true}` 当成功输出交回模型；（4）`findExistingToolCall` 补齐租约 fencing，与步骤重放路径对齐；（5）维护循环删除只写不读的 `activeExecutions`、密钥门禁统一为 ≥32 位、连续失败改抽样日志。附带把 `test` 脚本补上 `--experimental-vm-modules` 并清掉 AI 模块遗留 ESLint 问题。AI 模块单测 119 条通过、类型检查与 ESLint 通过；真实 PostgreSQL 16 条通过（新增孤儿排队 Run 恢复用例，并在工具审计用例内补充重放与租约 fencing 断言），既有 15 条无回归。2.4 标记为完成。 |
| 2026-08-25 | v2.1 | 完成 2.4-I～2.4-L 收尾：补齐可等待的 Runtime 生命周期、后继 Run 串行调度、Next.js 进程级过期租约对账；补齐 ToolCall/Step 关联、稳定输入指纹幂等重放、终态遗留工具收敛和描述/执行器双向校验；统一跨端 Runtime contracts，增加实时 `ai:chat:use`/`decision:read` 校验与 SSE Thread 边界；移除 Jest 全局开发环境加载及 `_to_delete` 旧副本。migration 与 Prisma Client 已就绪；2.4 定向单测 51 条、状态机 68 条、真实 PostgreSQL 15 条、Web Runtime/模型 10 条通过，contracts/Web 类型检查和修改文件 ESLint 通过。2.4 标记为完成，并把 2.5 拆为 A～D 四个可独立评审增量。 |
| 2026-08-25 | v2.0 | 按老大“执行完所有 2.4 计划”的要求，一次性完成 2.4 剩余全部增量：2.4-D `getDecisionContext`（decisions 模块新增 `DecisionContextService`，ai 侧薄执行器 + 来源标识）；2.4-E 新增 `AiStep`/`AiToolCall`/`AiSourceDependency` 三张表、两个工具事件类型与 migration `20260825140000`，配套步骤/工具调用/来源/助手消息/上下文预算服务；2.4-F 新增 `AiToolInvocationService` 与**声明式**串联规则 `discoveryRequirement`（只有本 Run 内唯一命中的候选才允许被读取）；2.4-G 新增共享密钥守卫、内部执行接口和浏览器 Run 接口；2.4-H 新增 Next.js Agent Runtime 模型循环、动态工具桥接、SSE 事件补拉和 BFF 路由。共享契约增加工具调用/来源类型与 5 个 AI 错误码，两个 `.env.example` 增加 `AI_RUNTIME_SERVICE_TOKEN`。**本轮没有执行任何 pnpm/prisma/测试命令**（设备侧 VM 读不了 Windows pnpm 目录联接，云端出网被拦截），因此 2.4 全部标记为 🧪 待验证。 |
| 2026-08-25 | v1.9 | 响应老大对 2.4-C 代码组织的反馈（“写在 ai 模块规范吗，后续 20+ 工具怎么办”），做架构调整而非新增功能：把 Decision 查询逻辑从 ai 模块下沉到 `decisions` 模块新增的 `DecisionDiscoveryService`（`decisions` 模块导出，ai 模块通过 `imports: [DecisionsModule]` 调用）；ai 模块侧改为薄执行器 `FindDecisionCandidatesToolService`，只做输入校验、权限上下文派生、委托查询和输出映射；工具描述与执行器按业务域拆分到 `ai/tools/decision/`，为后续更多工具定好目录模式，避免 `ai.module.ts` 和单个 service 文件随工具数量线性膨胀。旧的 `ai/services/ai-decision-discovery.service.ts`、对应单测和 `ai/types/ai-decision-discovery.types.ts` 已移出模块目录（因设备侧无删除权限，暂存于仓库根 `_to_delete/`，等待老大手动删除）。业务逻辑与查询条件等价，但代码路径变化较大，本次结构调整后 2.4-C 状态由 ✅ 改回 🧪，需要老大在本机跑通新路径下的验证命令后再确认。 |
| 2026-08-25 | v1.8 | 老大在本机验证 2.4-C：`pnpm --filter @nextnest/server test -- --runInBand modules/ai/policies/ai-permission-policy.spec.ts modules/ai/services/ai-decision-discovery.service.spec.ts modules/ai/services/ai-tool-registry.service.spec.ts` 3 个 Test Suite、10 条测试全部通过；`pnpm --filter @nextnest/server exec tsc --noEmit` 只剩既有 `meetings.service.spec.ts` 两处历史 fixture 类型错误，AI 新增/修改文件没有新增类型错误。2.4-C 状态由 🧪 待验证改为 ✅ 已完成。 |
| 2026-08-25 | v1.7 | 完成 2.4-C 代码实现：注册 `findDecisionCandidates` 工具描述；新增 `AiPermissionPolicyService` 从 `ownerUserId` 现取现算与登录态一致的授权上下文（用户缺失/非 ACTIVE 时拒绝）；新增 `AiDecisionDiscoveryService.findDecisionCandidates`，复用 `AuthorizationService.buildDecisionWhere('decision:read')` 在 SQL 层过滤未授权 Decision，按 `title` 模糊匹配 + 纯数字查询词按主键精确匹配，候选上限 5 条按 `updatedAt desc` 排序；新增 3 组定向单元测试，均 mock Prisma/授权服务、不连接真实数据库。本轮未能在当前会话环境实际跑通验证命令（设备桥接 VM 无法解析 Windows `pnpm` 安装产生的 NTFS 联接，云端沙箱出网白名单拒绝新增依赖下载），2.4-C 按规范标记为 🧪 待验证，需老大在本机确认。 |
| 2026-08-25 | v1.6 | 完成 2.4-B：新增中心只读工具注册表、稳定工具描述、窄输入/输出字段、超时和从已领取 Run 派生的受控用户上下文；启动时拒绝重复、写能力和不完整配置，并深拷贝冻结已注册描述。新增 3 条无数据库、模型或业务工具依赖的单元测试；未注册或执行任何真实工具。 |
| 2026-08-25 | v1.5 | 完成 2.4-A：新增 NestJS Agent Runtime 内部领取入口，冻结服务身份与 `runId` 输入，并从已领取 Run 重新读取用户、消息、模型角色和租约形成受控执行上下文；新增 3 条不依赖数据库、模型或工具的单元测试。本增量未接入内部 HTTP 鉴权、模型、工具、Decision 查询、BFF、流或 UI。 |
| 2026-08-25 | v1.4 | 使用老大授权的当前数据库连接执行 2.3 持久化测试，13 条真实 PostgreSQL 用例全部通过；测试仅清理 AI 会话相关表与临时测试用户。修正“最新调整方向”在旧排队 Run 取消后已创建新 `QUEUED Run` 时，用户消息投递状态应为 `DISPATCHED` 的测试断言。 |
| 2026-08-25 | v1.3 | 完成 2.3 代码实现：新增执行租约服务、Run 控制服务与事件 fencing；实现续租、停止/取消确认、定向取消、过期对账、队列释放和 13 条真实 PostgreSQL 竞争/故障测试。ESLint 和 contracts 类型检查通过；状态机 56 条通过；隔离数据库未配置导致持久化用例尚未执行，完整 Server 类型检查仅余既有 meetings fixture 错误。 |
| 2026-08-25 | v1.2 | 完成 2.3-A 代码：`AiRunService` 以条件更新原子领取 `QUEUED` Run，签发随机 30 秒执行租约并写入开始时间；新增真实 PostgreSQL 并发领取测试。当前没有 `AI_PERSISTENCE_TEST_DATABASE_URL`，因此新增测试按保护策略跳过；全量 Server 类型检查仍被既有 meetings fixture 类型错误阻塞。 |
| 2026-08-25 | v1.1 | 根据老大复核补齐独立 `AiMessage` 投递状态机和表驱动测试；队列事务在领取或替代消息前统一校验 `QUEUED -> DISPATCHED/SUPERSEDED` 合法性。 |
| 2026-08-25 | v1.0 | 完成 2.2-C：新增用户输入投递状态、队列序号、提交模式和 `USER_REDIRECTED`；活跃 Run 期间普通消息持久化排队，终态事务只领取队首；最新“调整方向”将全部未领取旧输入标记为 `SUPERSEDED`。`Sliye` 中仅 AI 表、AI enum 和旧 AI migration 记录经老大授权清理后重建；`20260825100000_add_ai_input_queue` 已应用，62 条定向测试通过。 |
| 2026-08-25 | v0.9 | 老大确认“调整方向”代表最新请求语义：最新定向输入会替代全部尚未领取的旧用户输入，而非只替代旧定向输入；旧普通和旧定向消息都保留为 `SUPERSEDED` 审计记录，不再执行。 |
| 2026-08-25 | v0.8 | 根据老大确认，2.4 从单一 `getDecisionContext` 技术夹具调整为“自然语言实体发现 + 决策上下文读取”的最小工具链：用户无需手输 `decisionId`；唯一候选才读取，重名/无结果时追问。讨论正文、会议转写等非结构化检索仍留在第 6、7 阶段。 |
| 2026-08-25 | v0.7 | 根据老大确认的“活跃 Run 期间排队发送 / 调整方向”交互新增 2.2-C：普通输入持久化排队；定向输入请求取消当前 Run 后优先续跑，不向正在执行的模型请求硬注入文本。同步补充 2.3 取消与 fencing、2.5 API、2.6 现有输入框接入、数据模型、测试矩阵和待决策项；本次仅更新计划，未实现功能。 |
| 2026-08-25 | v0.6 | 完成 2.2：新增 AI 核心 Prisma 模型和 migration；落地 Thread 活跃指针、非终态部分唯一索引、三类资源级幂等、原子事件序号与真实 PostgreSQL 并发测试；验证 migration 应用及 reset 后重新应用 |
| 2026-08-25 | v0.5 | 根据老大复核移除 Thread 可选入口上下文：业务目标改由用户消息表达、Agent 发现与消歧；删除入口字段、Thread 锁定状态、对应错误码和已失去意义的规则测试 |
| 2026-08-25 | v0.4 | 完成 2.1：落地可选入口上下文契约、Thread 访问状态与锁定原因、稳定错误码和入口上下文纯规则；定向类型检查及 67 条状态测试通过 |
| 2026-08-25 | v0.3 | 移除容易被误解为全局权限的 `GLOBAL Thread`/`GLOBAL` 枚举；统一改为普通 Thread + 可选 PROJECT/DECISION/MEETING 入口上下文，全部为空表示从 AI 工作台创建 |
| 2026-08-25 | v0.2 | 明确 AI 工作台第一版 UI 已完成；2.6 改为在现有风格内接入真实数据和状态，不重新设计页面，并新增视觉与功能完整性验收 |
| 2026-08-25 | v0.1 | 从总路线图抽离第二阶段；根据最终全局 Agent 目标修正单 Decision Thread 限制；新增完成状态、数据约束、测试矩阵和待决策项 |
